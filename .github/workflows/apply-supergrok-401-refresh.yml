name: Apply SuperGrok 401 refresh patch

on:
  push:
    branches:
      - fix/supergrok-401-auth-recovery

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/supergrok-401-auth-recovery
          fetch-depth: 0
      - name: Apply deterministic patch
        run: python scripts/apply-supergrok-401-refresh.py
      - name: Commit patch and remove one-shot machinery
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: force-refresh SuperGrok after upstream 401'
          git push origin HEAD:fix/supergrok-401-auth-recovery
