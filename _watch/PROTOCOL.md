# GLM 5.3 ↔ web master seo watcher

Sharon does not paste. Instructor (web master seo) drives FreeBuff and writes `_watch/INSTRUCTOR.md`.

- Instructor writes `_watch/INSTRUCTOR.md` (the only orders that count). One turn = one complete job.
- GLM reads INSTRUCTOR.md, implements, runs the named checks, then writes `_watch/GLM.md` (summary, files, git status, tsc/jest, blocked/done).
- GLM does **not** commit or push unless INSTRUCTOR.md says `COMMIT: yes` with the exact message and file list.
- After writing GLM.md, STOP. Do not invent the next task.
- Do not edit `_watch/PROTOCOL.md`. Do not stage `_watch/` unless told.
- Do the whole job in one sitting. Do not stop mid-job to wait for chat.
