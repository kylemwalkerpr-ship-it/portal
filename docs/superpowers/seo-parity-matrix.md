# SEO Cleanup → Expansion Parity Matrix

This matrix is the phase gate for `docs/superpowers/specs/2026-09-15-seo-cleanup-expansion-parity-design.md`.

Allowed statuses: `PENDING`, `IN_PROGRESS`, `PASS`, `ACCEPTED_EXCEPTION`, `BLOCKED_EXTERNAL`, `FAIL`.

Evidence must name the command/query/artifact that proves the state. An agent assertion is not evidence.

| Phase | Requirement | Status | Current evidence / next proof |
|---|---|---|---|
| P0 | Local `main` = fetched `origin/main` = authoritative remote `main` | PASS | 2026-09-15 P0 refresh immediately before final verification: worktree base `HEAD`, fetched `origin/main`, and authoritative `git ls-remote origin refs/heads/main` all equal `c51f99046c122dd3ff34f29afd120996cb8efede`. |
| P0 | Control branch contains only control-plane docs before implementation | PASS | `seo/cleanup-expansion-parity-20260915` is `0` behind / `1` ahead of main at `6329c88a60149f31c655ef3e77af12c681a99b09` before this plan/matrix/ledger commit. |
| P0 | Stale branch deletion queue is exact-tip verified | PASS | 2026-09-15 Task 1 (supervisor): 33 branches deleted only after exact-tip proof — 4 direct/reachable safe deletions plus 29 squash-merge deletions whose CURRENT tip exactly matched its merged PR head and whose reachable main landing commit had an identical Git tree (original PR heads NOT claimed reachable); tree identity proves no unique file state under AGENTS.md. Total deleted=33, remain=47. |
| P0 | Unique/active branches explicitly retained | PASS | 2026-09-15 Task 1 (supervisor): active/unique/ambiguous branches retained, including `seo/gsc-soft404-canonicalization`. |
| P0 | Marketplace category URL generation uses public market host + clean `/categories/<id>` | PASS | `lib/marketplaceSeo.ts` `marketplaceCategoryHref` builds `https://market.yousafeconsultancy.com/categories/<id>` (blank input falls back to `immigration`); `lib/seoEngine/interlink.ts` derives `ESTATE_BASE.market` from the shared canonical helper. Proof: `npx jest tests/seo-estate-public-url-contract.test.ts` — 7/7 pass (2026-09-15); `npx tsc --noEmit` exit 0; `npm run build` exit 0. Runtime source scan: no executable emission of the retired path (only the reconciliation script's retired-prefix constant and docs). |
| P0 | LLM citation estate recognizes public Marketplace host | PASS | `lib/seoEngine/llmVisibility.ts` `ESTATE_DOMAINS` includes `market.yousafeconsultancy.com`; Portal retained as legitimate Portal/auth surface. Proven by `tests/seo-estate-public-url-contract.test.ts` (7/7 pass, 2026-09-15). |
| P0 | Legitimate Portal/auth identity remains distinguishable from Marketplace canonical | PASS | `tests/seo-estate-public-url-contract.test.ts` asserts `ESTATE_DOMAINS` contains both hosts and that the market base is never Portal; runtime scan shows remaining `portal.yousafeconsultancy.com` references in `lib/` are Portal/auth flows (email, clerk, GA4, war-room footer, contact) or the accepted `seoFactory/ownership.ts` legacy-input alias — no Marketplace canonical emission. |
| P0 | Existing stale planned interlinks detected/reconciled | PASS | Task 4: `scripts/reconcile-stale-planned-interlinks.mts` is strictly read-only evidence tooling (query/validate/map/rollback output; no update path). Live dry-run `affected_count=70, malformed_count=0, collision_count=0` saved at `/tmp/p0-interlink-reconcile-dryrun`. Supervisor applied atomically via SQL 2026-09-15: retired planned rows now 0; exactly 70 clean planned rows remain (35 immigration, 17 study-permits, 18 work-permits); wrong target_host=0; none marked applied. |
| P1 | Task 1 boundary: `support_notify(uuid,text,text,text,text,text)` and `support_log_action(uuid,text,text,text,text,jsonb)` are executable only by `service_role`, and `content_job_health_summary` / `inquiry_engagement` / `seo_backlink_dashboard` / `support_user_notes_v` are `security_invoker` views selectable only by `service_role` | PASS | Narrow claim only — does not assert base-table least-privilege (tracked in the next row). Local acceptance (2026-09-15): `tests/support-security-boundary.test.ts` 3/3 pass (exact `to_regprocedure`/`to_regclass` guards); `tests/migration-order.test.ts` 8/8 pass; `npx tsc --noEmit` exit 0. Production acceptance: PR #203 squash-merged to `main` as `9a21bfab291d5f381a2fce765a77a949a4d5ad8e`; `Deploy YouSafe Portal` run `35024648249` completed success including Cloudflare deploy, secrets health, and post-deploy smoke test; post-maintenance production SQL shows both RPCs EXECUTE `anon=false`, `authenticated=false`, `service_role=true`, and all four views SELECT `anon=false`, `authenticated=false`, `service_role=true` with `security_invoker=true` in reloptions. Security advisors at `2026-09-15T21:45:45Z` no longer list either support RPC in `anon`/`authenticated` SECURITY DEFINER warnings and no longer list the four views as `security_definer_view` findings. Migration run `35024648323` overall failed on unrelated older migrations (`20260911_marketplace_search_intelligence.sql`: `42P16` cannot drop columns from view; `content_jobs_fts_index.sql`: HTTP 503 scheduled maintenance) but its log proves `20260915_support_security_boundary.sql = OK`; the migration workflow is NOT green overall. Unrelated security-advisor backlog remains. |
| P1 | Base-table grants/RLS are least-privilege for exactly `content_jobs`, `seo_backlink_targets`, `seo_backlink_outreach`, and `support_audit_log` | PASS | Historical pre-state (2026-09-15; superseded by the 2026-09-17 production acceptance below): Separate from the Task 1 RPC/view boundary; broad base-table hardening is out of scope for Task 1. Live effective anon reads (2026-09-15): `content_jobs` 240 rows; `seo_backlink_targets` 14/14 rows; `seo_backlink_outreach` 14/14 rows; `support_audit_log` anon SELECT privilege/policy shape is exposed, but its current effective anon row count was 0 (no rows leaked). Compatibility constraint: `content_jobs` cannot be locked to `service_role` in this task because the live Content Studio browser Realtime client subscribes to `public.content_jobs` through the anon Supabase client (`lib/supabaseRealtime.ts` → `createSupabaseBrowserClient()`; live caller `components/design/admin-content-studio.tsx:7533`, mounted via `components/design/admin.jsx:19`), so any fix must be Realtime-compatible. Backlink tables are used server-side via `createSupabaseAdminClient()` (`lib/seoEngine/backlinkEngine.ts`); `support_audit_log` is used server-side (`app/api/admin/users/[id]/route.ts`). Production acceptance 2026-09-17 (narrow claim: exactly `public.content_jobs`, `public.seo_backlink_targets`, `public.seo_backlink_outreach`, `public.support_audit_log` only): implementation PR #222 head `d51a815f78fee2d8783caeb5dd15d69840014c08` passed `Content Studio Review` run #125 and the `Deploy YouSafe Portal` PR-context run #2894, then merged to `main` as `5429e5a124aa25df7d771fada50741a234a21d3b`. Main `Apply SEO Factory Migrations` run `35257551755` (run #33) succeeded, and `supabase_migrations.yousafe_migration_ledger` records `20260917173300_base_table_least_privilege.sql` applied by `ci-runner` at `2026-09-17T18:14:03.941539+00:00` with `source_git_sha=5429e5a124aa25df7d771fada50741a234a21d3b`. Main `Deploy YouSafe Portal` run `35257551736` succeeded including typecheck, unit tests, build, SEO guard, Cloudflare deploy, secrets health, and post-deploy smoke. Live SQL proof after the migration: all four tables have RLS enabled; each has exactly one policy named `Service role full access`, cmd `ALL`, roles `{service_role}`, `qual`/`with_check` true; `anon_select=false`, `authenticated_select=false`, `anon_any_column_select=false`, `authenticated_any_column_select=false`; `service_role_select=true` and `service_role_write=true`; table grants show only `service_role` with `DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE`. Effective role simulation as `anon` against `content_jobs` returned permission denied. Production authenticated Content Studio proof after deploy: `/dashboard/admin/content` loaded 246 jobs in the table and the network panel showed repeated `GET /api/content-studio/jobs?limit=100` => 200 and `GET /api/content-studio/jobs?limit=80&status=drafting,pending,publishing,pr_created,merged,failed` => 200, demonstrating the authenticated API polling fallback is operational after removing the direct browser `content_jobs` Realtime subscription. Local acceptance from the implementation: new regression `tests/p1-base-table-least-privilege.test.ts` 16/16; focused security/migration suites 6 suites / 60 tests; `npx tsc --noEmit` pass; `npm run build` pass; `git diff --check` pass; independent DeepSeek review NO BLOCKER/HIGH. Unrelated claim preserved as code acceptance only: the 7-table SEO telemetry Realtime subscription (`seo_knowledge`, `seo_cluster_plans`, `seo_interlinks`, `seo_llm_visibility`, `seo_gate_runs`, `seo_engine_runs`, `seo_ranking_scores`) is pinned by `tests/p1-base-table-least-privilege.test.ts`; no production Realtime publication proof is claimed for it. This PASS is scoped to this row only; overall P1 remains incomplete and the `Qualified visibility separated from raw off-mission visibility`, `LLM audit failures excluded from genuine citation-loss math`, and `Reward/forecast inputs are tied to real observations` rows remain PENDING. See `docs/superpowers/seo-execution-ledger.md` → 2026-09-17 production acceptance section. |
| P1 | Fresh GSC query×page data is reproducible | PASS | Narrow definition (does not claim live Google metric values are immutable between reruns, and does not define reproducibility as a durable guarantee of no stale keys): reproducible = the sync deterministically selects the stable UTC window and idempotently upserts the current response key space without duplicate unique keys, and truthfully reports live/empty/unavailable/failed; the 2026-09-17 production run additionally observed 0 stale pre-existing keys, which is run-scoped evidence, not an enforced invariant (see Limitations). Spec gate: `docs/superpowers/specs/2026-09-15-seo-cleanup-expansion-parity-design.md` P1 measurement-integrity gate requires "reproducible current GSC rows" and "missing provider/API data is explicitly `unavailable`, not `0`", with goal "prove GSC sync freshness". Production 2026-09-17: authenticated admin UI "Sync GSC (90d)" issued `POST /api/content-studio/gsc/sync`, returning HTTP 200 in ~5.2s with `ok=true`, `status=live`, `rowsProcessed=2668`, range 2026-06-19→2026-09-16 days=90, `siteUrl=sc-domain:yousafeconsultancy.com`, `source=live`, `syncedAt=2026-09-17T16:21:37.752Z`, `attemptedAt=2026-09-17T16:21:32.898Z`, `warnings=[]`. Before sync, the same exact site/window held 2663 rows = 2663 distinct(query,page), clicks=2, impressions=35748, `synced_at=15:36:12.141Z`; after sync it held 2668 rows = 2668 distinct(query,page), `duplicate_unique_keys=0`, clicks=2, impressions=36070, `synced_at=16:21:34.142Z`; the 2026-09-17 production anti-stale query for that site/window with `synced_at < attemptedAt` proved 0 stale pre-existing keys for that run; total `seo_gsc_rows` after sync 20017, all query+page populated, max end_date 2026-09-16. Persisted-read UI showed "seo_gsc_rows: 40 persisted · window 2026-06-19 → 2026-09-16 · showing top 40 · 40 act-on rows merged into the work plan" and "Last sync: 2,668 rows upserted · 2026-06-19 → 2026-09-16 · source live" — this UI is a top-40 session slice, while the SQL above is the full-window proof. Local supervisor command: `npx --no-install jest tests/gsc-sync-route-truth.test.ts tests/gsc-persistence.test.ts tests/gsc-rows.test.ts tests/seo-engine-daily-gsc-persist.test.ts --runInBand` → 4 suites / 32 tests pass. Code artifacts: `lib/gscAnalytics.ts` `resolveGscDayWindow` anchors endDate to yesterday UTC from one timestamp (same-UTC-day calls yield the identical window); `lib/seoFactory/gscRows.ts` `paginateGscDimensionPages` pages 5k rows under a 25k cap and `upsertSeoGscRows` conflicts on `site_url,query,page,start_date,end_date`; `lib/seoFactory/gscPersistence.ts` statuses are exactly live/empty/unavailable/failed — field-level metric defaults to 0 (e.g. `lib/gscAnalytics.ts` `r.clicks ?? 0`) are provider-row normalization of missing provider fields within a returned row, while dataset-level unavailable/failed states remain truthful (missing provider/API data is reported as `unavailable`, not `0`). Limitations: the implementation is upsert-only (`upsertSeoGscRows` upserts on `site_url,query,page,start_date,end_date` with no delete/prune path), so keys that later disappear from GSC for the same window are not removed — stale-key absence is observed for the 2026-09-17 run only, not enforced as a durable invariant; the 25k query×page cap (`paginateGscDimensionPages`) has no truncation warning today and this 90d window (2668 rows) was not truncated, so a truncated or later-shrunken provider response can leave prior-run keys for the same window in place (the cap bounds each run's fetched key space, not the table's residual key space); same-window metric values can mature (2663→2668 rows, 35748→36070 impressions) because GSC data is live/lagged — reproducibility is about deterministic window selection, idempotent upsert of the current key space, and truthful status, not frozen values; max end_date 2026-09-16 is a window label, not proof every day inside it is finalized. PASS is scoped to this row only; overall P1 is not complete and other P1 rows remain PENDING. |
| P1 | Priority index/index-coverage evidence is operational | PASS | Branch `seo/parity-p1-index-coverage-20260916` (base `56690c702801c0f20d1ceebd8bb7ccb67950423a`): the accepted five-file P1B implementation is committed as `84bfb40a435389c6e8bde5749d49b3272d3bea78` with message `feat(seo): make index coverage measurement truthful`; candidate-scoped prior-history reads are greedily chunked under `INSPECTION_FILTER_BUDGET_BYTES=2000` instead of one unbounded PostgREST `.in` query, and no global `limit(5000)` remains. The 5100-candidate regression reads exact history in ≤200 chunks (actual ~165), preserves never-inspected-first then oldest-inspected ordering, and cannot pass under a one-URL-per-chunk regression (5100 chunks); no candidate is misclassified by global 5000-row truncation. History-read errors fail closed before GSC URL Inspection and record `selectionStrategy='unavailable'`, `priorHistoryAvailable=false`, and truthful actual `priorHistoryReads` (attempted reads; 0 if client creation throws). `maxUrls` validation rejects explicit null/string/zero/negative/fractional before any inventory/inspection/metadata side effects, missing `maxUrls` defaults to 50, and values >50 clamp to 50. Successful observations, including indexed URLs, persist; partial/failure semantics and run metadata `kind='manual'` plus `summary.measurementKind='index-coverage'` remain (no schema migration; the production migration and its `seo_engine_runs` / `gsc_index_coverage` tables already exist and are protected). Lightweight estate inventory remains tree-only (no page blobs). Subrequest note: history chunk reads add to the ≤50 URL inspections; the 5100 stress fixture is ~165 history reads plus inventory/inspection overhead and stays under the repo-configured `wrangler.toml` subrequests=5000, superseding the earlier understated total. Fresh supervisor evidence (2026-09-17, local): `git diff --check` exit 0; focused suites `tests/gsc-index-coverage-route-truth.test.ts` + `tests/gsc-index-coverage-truth.test.ts` + `tests/site-health-inventory.test.ts` 3 suites / 30 tests pass; `npx tsc --noEmit` exit 0; full Jest 405 suites passed / 2 skipped (4304 tests passed / 4 skipped) exit 0; `npm run build` exit 0 with Next.js compile + TypeScript + OpenNext Cloudflare bundle and `.open-next/worker.js` generated; post-build generated SEO data diffs were restored, leaving exactly five intended modified files (`app/api/content-studio/gsc/index-coverage/route.ts`, `docs/superpowers/seo-parity-matrix.md`, `lib/gscIndexCoverage.ts`, `tests/gsc-index-coverage-route-truth.test.ts`, `tests/gsc-index-coverage-truth.test.ts`). Two independent read-only DeepSeek reviews found no material blockers after the bounded-history fix. Caveat: results are a bounded max-50 URL Inspection sample, never estate-wide zero. Production completion proof (2026-09-17): PR #218 merged the truthful index-coverage implementation to `main` as merge commit `39040b8500959a73c3a7922c2325e77f37690ffa`. PR #219 mounted the admin Search Console/Indexing UI and fixed property-query truth; the exact reviewed head `f4fb9e65ba19a5a13d4b38f5e2d5f28a5e75c640` merged to `main` as `3f896ae727801e40816b62cd91cd69e2373ebcef`. PR-head workflows on #219: `Content Studio Review` success; `Deploy YouSafe Portal` PR-context checks passed typecheck, unit tests, build, and SEO audit. Main push `Deploy YouSafe Portal` run #2889 (run id `35240269227`) for merge SHA `3f896ae727801e40816b62cd91cd69e2373ebcef` completed success including typecheck, unit tests, Next/OpenNext build, SEO audit, Cloudflare deploy, secrets sync/health, and post-deploy smoke. Authenticated production URL `/dashboard/admin/gsc` rendered Search Console, service-account connected, Indexing tab, cached list data (the cached list path showed prior `CRAWLED_NOT_INDEXED` evidence), and the Scan index coverage control. Live scan completion UI: "URL Inspection sample partial: 45/50 successful · 28 actionable issues · 5 failed. Not an estate-wide coverage total." Supabase `seo_engine_runs` row id `ec8c451e-01cf-49fe-9eff-96cba119ab28`: status `partial`, `measurementKind=index-coverage`, `measurementState=partial`, `scope=all`, `maxUrls=50`, `candidateCount=528`, `requestedUrlCount=50`, `attemptedUrlCount=50`, `inspectedUrlCount=45`, `failedCount=5`, `cachePersisted=true`, `priorHistoryAvailable=true`, `priorHistoryRows=1`, `priorHistoryReads=23`, `selectionStrategy=never-inspected-first_then_oldest-inspected`, `siteUrl=sc-domain:yousafeconsultancy.com`; started `2026-09-17 15:42:26.408+00` and finished `2026-09-17 15:44:39+00`. `gsc_index_coverage` after the scan: 46 rows total, 17 indexed, 29 non-indexed, `latest_inspected_at` `2026-09-17 15:44:39+00`. This proves a truthful bounded max-50 sample, partial failure semantics, cache persistence of successful indexed + non-indexed observations, and run metadata; it does not prove estate-wide coverage or 100% success. PASS here means only that the operational bounded-sample index-coverage requirement is proven; it is not a claim that P1 is complete. Overall SEO parity remains RED; multiple other P1 rows remain PENDING. |
| P1 | Qualified visibility separated from raw off-mission visibility | PENDING | Phase not started. |
| P1 | LLM audit failures excluded from genuine citation-loss math | PENDING | Existing architecture claims this; phase must prove current behavior/data. |
| P1 | Reward/forecast inputs are tied to real observations | PENDING | Phase not started. |
| P2 | Sitemap contains zero invalid public URLs | PENDING | Phase not started. |
| P2 | Meaningful canonical conflicts = 0 | PENDING | Phase not started. |
| P2 | Redirect/4xx/parameter duplicates/broken internal links reconciled | PENDING | Phase not started. |
| P2 | Priority indexable orphans resolved or justified | PENDING | Phase not started. |
| P3 | 100% strategic intents have one owner | PENDING | Phase not started. |
| P3 | ≥95% active priority intents mapped | PENDING | Phase not started. |
| P3 | CREATE is blocked without owner resolution | PENDING | Phase not started. |
| P4 | Major priority-cluster cannibalization resolved | PENDING | Phase not started. |
| P5 | High-impression off-mission URLs have explicit disposition | PENDING | Phase not started. |
| P5 | Off-mission pages cannot generate missions | PENDING | Phase not started. |
| P6 | Approved internal-link backlog is verified live or rejected/stale | PENDING | Target ≥80%; DB `applied` alone is not proof. |
| P7 | Near-win ranking interventions have before/after evidence | PENDING | Phase not started. |
| P8 | Trust/E-E-A-T surfaces are truthful and source-fresh | PENDING | Phase not started. |
| P9 | External authority operations record verified wins | PENDING | Phase not started. |
| P10 | Conversion attribution is instrumented and truthful | PENDING | Unknown conversion data must remain unknown, never inferred. |
| P11 | GEO/AI visibility evidence is current and failure-aware | PENDING | Phase not started. |
| P12 | Full estate validation gates pass | PENDING | Phase not started. |
| P13 | Controlled expansion passes evidence/content/ownership gates | PENDING | Phase not started. |

## Status discipline

- `PASS`: direct reproducible evidence exists and remains current for the phase decision.
- `ACCEPTED_EXCEPTION`: deliberate exception with owner, rationale, and bounded risk.
- `BLOCKED_EXTERNAL`: required provider/data/system is unavailable; evidence proves the dependency failure.
- `FAIL`: evidence contradicts the target contract.
- `PENDING`/`IN_PROGRESS`: cannot advance the phase.

Every implementation PR updates this matrix before handoff to GPT-5.6 Sol.
