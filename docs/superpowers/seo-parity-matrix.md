# SEO Cleanup → Expansion Parity Matrix

This matrix is the phase gate for `docs/superpowers/specs/2026-09-15-seo-cleanup-expansion-parity-design.md`.

Allowed statuses: `PENDING`, `IN_PROGRESS`, `PASS`, `ACCEPTED_EXCEPTION`, `BLOCKED_EXTERNAL`, `FAIL`.

Evidence must name the command/query/artifact that proves the state. An agent assertion is not evidence.

| Phase | Requirement | Status | Current evidence / next proof |
|---|---|---|---|
| P0 | Local `main` = fetched `origin/main` = authoritative remote `main` | PASS | 2026-09-15 P0 refresh immediately before final verification: worktree base `HEAD`, fetched `origin/main`, and authoritative `git ls-remote origin refs/heads/main` all equal `c51f99046c122dd3ff34f29afd120996cb8efede`. |
| P0 | Control branch contains only control-plane docs before implementation | PASS | `seo/cleanup-expansion-parity-20260915` is `0` behind / `1` ahead of main at `6329c88a60149f31c655ef3e77af12c681a99b09` before this plan/matrix/ledger commit. |
| P0 | Stale branch deletion queue is exact-tip verified | PASS | 2026-09-15 Task 1 (supervisor): 33 branches deleted only after exact-tip proof — 4 direct/reachable safe deletions plus 29 squash-merge deletions whose CURRENT tip exactly matched its merged PR head and whose reachable main landing commit had an identical Git tree (original PR heads NOT claimed reachable); tree identity proves no unique file state under AGENTS.md. Total deleted=33, remain=47. |
| P0 | Unique/active branches explicitly retained | PASS | 2026-09-15 Task 1 (supervisor): active/unique/ambiguous branches retained, including `seo/gsc-soft404-canonicalization`. |
| P0 | Marketplace category URL generation uses public market host + clean `/categories/<id>` | PASS | `lib/marketplaceSeo.ts` `marketplaceCategoryHref` builds `https://market.yousafeconsultancy.com/categories/<id>` (blank input falls back to `immigration`); `lib/seoEngine/interlink.ts` derives `ESTATE_BASE.market` from the shared canonical helper. Proof: `npx jest tests/seo-estate-public-url-contract.test.ts` — 7/7 pass (2026-09-15); `npx tsc --noEmit` exit 0; `npm run build` exit 0. Runtime source scan: no executable emission of the retired path (only the reconciliation script's retired-prefix constant and docs). |
| P0 | LLM citation estate recognizes public Marketplace host | PASS | `lib/seoEngine/llmVisibility.ts` `ESTATE_DOMAINS` includes `market.yousafeconsultancy.com`; Portal retained as legitimate Portal/auth surface. Proven by `tests/seo-estate-public-url-contract.test.ts` (7/7 pass, 2026-09-15). |
| P0 | Legitimate Portal/auth identity remains distinguishable from Marketplace canonical | PASS | `tests/seo-estate-public-url-contract.test.ts` asserts `ESTATE_DOMAINS` contains both hosts and that the market base is never Portal; runtime scan shows remaining `portal.yousafeconsultancy.com` references in `lib/` are Portal/auth flows (email, clerk, GA4, war-room footer, contact) or the accepted `seoFactory/ownership.ts` legacy-input alias — no Marketplace canonical emission. |
| P0 | Existing stale planned interlinks detected/reconciled | PASS | Task 4: `scripts/reconcile-stale-planned-interlinks.mts` is strictly read-only evidence tooling (query/validate/map/rollback output; no update path). Live dry-run `affected_count=70, malformed_count=0, collision_count=0` saved at `/tmp/p0-interlink-reconcile-dryrun`. Supervisor applied atomically via SQL 2026-09-15: retired planned rows now 0; exactly 70 clean planned rows remain (35 immigration, 17 study-permits, 18 work-permits); wrong target_host=0; none marked applied. |
| P1 | Task 1 boundary: `support_notify(uuid,text,text,text,text,text)` and `support_log_action(uuid,text,text,text,text,jsonb)` are executable only by `service_role`, and `content_job_health_summary` / `inquiry_engagement` / `seo_backlink_dashboard` / `support_user_notes_v` are `security_invoker` views selectable only by `service_role` | PASS | Narrow claim only — does not assert base-table least-privilege (tracked in the next row). Local acceptance (2026-09-15): `tests/support-security-boundary.test.ts` 3/3 pass (exact `to_regprocedure`/`to_regclass` guards); `tests/migration-order.test.ts` 8/8 pass; `npx tsc --noEmit` exit 0. Production acceptance: PR #203 squash-merged to `main` as `9a21bfab291d5f381a2fce765a77a949a4d5ad8e`; `Deploy YouSafe Portal` run `35024648249` completed success including Cloudflare deploy, secrets health, and post-deploy smoke test; post-maintenance production SQL shows both RPCs EXECUTE `anon=false`, `authenticated=false`, `service_role=true`, and all four views SELECT `anon=false`, `authenticated=false`, `service_role=true` with `security_invoker=true` in reloptions. Security advisors at `2026-09-15T21:45:45Z` no longer list either support RPC in `anon`/`authenticated` SECURITY DEFINER warnings and no longer list the four views as `security_definer_view` findings. Migration run `35024648323` overall failed on unrelated older migrations (`20260911_marketplace_search_intelligence.sql`: `42P16` cannot drop columns from view; `content_jobs_fts_index.sql`: HTTP 503 scheduled maintenance) but its log proves `20260915_support_security_boundary.sql = OK`; the migration workflow is NOT green overall. Unrelated security-advisor backlog remains. |
| P1 | Base-table grants/RLS are least-privilege for exactly `content_jobs`, `seo_backlink_targets`, `seo_backlink_outreach`, and `support_audit_log` | PASS | Historical pre-state (2026-09-15; superseded by the 2026-09-17 production acceptance below): Separate from the Task 1 RPC/view boundary; broad base-table hardening is out of scope for Task 1. Live effective anon reads (2026-09-15): `content_jobs` 240 rows; `seo_backlink_targets` 14/14 rows; `seo_backlink_outreach` 14/14 rows; `support_audit_log` anon SELECT privilege/policy shape is exposed, but its current effective anon row count was 0 (no rows leaked). Compatibility constraint: `content_jobs` cannot be locked to `service_role` in this task because the live Content Studio browser Realtime client subscribes to `public.content_jobs` through the anon Supabase client (`lib/supabaseRealtime.ts` → `createSupabaseBrowserClient()`; live caller `components/design/admin-content-studio.tsx:7533`, mounted via `components/design/admin.jsx:19`), so any fix must be Realtime-compatible. Backlink tables are used server-side via `createSupabaseAdminClient()` (`lib/seoEngine/backlinkEngine.ts`); `support_audit_log` is used server-side (`app/api/admin/users/[id]/route.ts`). Production acceptance 2026-09-17 (narrow claim: exactly `public.content_jobs`, `public.seo_backlink_targets`, `public.seo_backlink_outreach`, `public.support_audit_log` only): implementation PR #222 head `d51a815f78fee2d8783caeb5dd15d69840014c08` passed `Content Studio Review` run #125 and the `Deploy YouSafe Portal` PR-context run #2894, then merged to `main` as `5429e5a124aa25df7d771fada50741a234a21d3b`. Main `Apply SEO Factory Migrations` run `35257551755` (run #33) succeeded, and `supabase_migrations.yousafe_migration_ledger` records `20260917173300_base_table_least_privilege.sql` applied by `ci-runner` at `2026-09-17T18:14:03.941539+00:00` with `source_git_sha=5429e5a124aa25df7d771fada50741a234a21d3b`. Main `Deploy YouSafe Portal` run `35257551736` succeeded including typecheck, unit tests, build, SEO guard, Cloudflare deploy, secrets health, and post-deploy smoke. Live SQL proof after the migration: all four tables have RLS enabled; each has exactly one policy named `Service role full access`, cmd `ALL`, roles `{service_role}`, `qual`/`with_check` true; `anon_select=false`, `authenticated_select=false`, `anon_any_column_select=false`, `authenticated_any_column_select=false`; `service_role_select=true` and `service_role_write=true`; table grants show only `service_role` with `DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE`. Effective role simulation as `anon` against `content_jobs` returned permission denied. Production authenticated Content Studio proof after deploy: `/dashboard/admin/content` loaded 246 jobs in the table and the network panel showed repeated `GET /api/content-studio/jobs?limit=100` => 200 and `GET /api/content-studio/jobs?limit=80&status=drafting,pending,publishing,pr_created,merged,failed` => 200, demonstrating the authenticated API polling fallback is operational after removing the direct browser `content_jobs` Realtime subscription. Local acceptance from the implementation: new regression `tests/p1-base-table-least-privilege.test.ts` 16/16; focused security/migration suites 6 suites / 60 tests; `npx tsc --noEmit` pass; `npm run build` pass; `git diff --check` pass; independent DeepSeek review NO BLOCKER/HIGH. Unrelated claim preserved as code acceptance only: the 7-table SEO telemetry Realtime subscription (`seo_knowledge`, `seo_cluster_plans`, `seo_interlinks`, `seo_llm_visibility`, `seo_gate_runs`, `seo_engine_runs`, `seo_ranking_scores`) is pinned by `tests/p1-base-table-least-privilege.test.ts`; no production Realtime publication proof is claimed for it. Historical note: this row was accepted before the later P1 closures. As of 2026-09-19 all current P1 rows in this matrix are PASS; overall SEO parity remains incomplete because P2-P13 remain PENDING. See `docs/superpowers/seo-execution-ledger.md` → 2026-09-17 production acceptance section. |
| P1 | Fresh GSC query×page data is reproducible | PASS | Narrow definition (does not claim live Google metric values are immutable between reruns, and does not define reproducibility as a durable guarantee of no stale keys): reproducible = the sync deterministically selects the stable UTC window and idempotently upserts the current response key space without duplicate unique keys, and truthfully reports live/empty/unavailable/failed; the 2026-09-17 production run additionally observed 0 stale pre-existing keys, which is run-scoped evidence, not an enforced invariant (see Limitations). Spec gate: `docs/superpowers/specs/2026-09-15-seo-cleanup-expansion-parity-design.md` P1 measurement-integrity gate requires "reproducible current GSC rows" and "missing provider/API data is explicitly `unavailable`, not `0`", with goal "prove GSC sync freshness". Production 2026-09-17: authenticated admin UI "Sync GSC (90d)" issued `POST /api/content-studio/gsc/sync`, returning HTTP 200 in ~5.2s with `ok=true`, `status=live`, `rowsProcessed=2668`, range 2026-06-19→2026-09-16 days=90, `siteUrl=sc-domain:yousafeconsultancy.com`, `source=live`, `syncedAt=2026-09-17T16:21:37.752Z`, `attemptedAt=2026-09-17T16:21:32.898Z`, `warnings=[]`. Before sync, the same exact site/window held 2663 rows = 2663 distinct(query,page), clicks=2, impressions=35748, `synced_at=15:36:12.141Z`; after sync it held 2668 rows = 2668 distinct(query,page), `duplicate_unique_keys=0`, clicks=2, impressions=36070, `synced_at=16:21:34.142Z`; the 2026-09-17 production anti-stale query for that site/window with `synced_at < attemptedAt` proved 0 stale pre-existing keys for that run; total `seo_gsc_rows` after sync 20017, all query+page populated, max end_date 2026-09-16. Persisted-read UI showed "seo_gsc_rows: 40 persisted · window 2026-06-19 → 2026-09-16 · showing top 40 · 40 act-on rows merged into the work plan" and "Last sync: 2,668 rows upserted · 2026-06-19 → 2026-09-16 · source live" — this UI is a top-40 session slice, while the SQL above is the full-window proof. Local supervisor command: `npx --no-install jest tests/gsc-sync-route-truth.test.ts tests/gsc-persistence.test.ts tests/gsc-rows.test.ts tests/seo-engine-daily-gsc-persist.test.ts --runInBand` → 4 suites / 32 tests pass. Code artifacts: `lib/gscAnalytics.ts` `resolveGscDayWindow` anchors endDate to yesterday UTC from one timestamp (same-UTC-day calls yield the identical window); `lib/seoFactory/gscRows.ts` `paginateGscDimensionPages` pages 5k rows under a 25k cap and `upsertSeoGscRows` conflicts on `site_url,query,page,start_date,end_date`; `lib/seoFactory/gscPersistence.ts` statuses are exactly live/empty/unavailable/failed — field-level metric defaults to 0 (e.g. `lib/gscAnalytics.ts` `r.clicks ?? 0`) are provider-row normalization of missing provider fields within a returned row, while dataset-level unavailable/failed states remain truthful (missing provider/API data is reported as `unavailable`, not `0`). Limitations: the implementation is upsert-only (`upsertSeoGscRows` upserts on `site_url,query,page,start_date,end_date` with no delete/prune path), so keys that later disappear from GSC for the same window are not removed — stale-key absence is observed for the 2026-09-17 run only, not enforced as a durable invariant; the 25k query×page cap (`paginateGscDimensionPages`) has no truncation warning today and this 90d window (2668 rows) was not truncated, so a truncated or later-shrunken provider response can leave prior-run keys for the same window in place (the cap bounds each run's fetched key space, not the table's residual key space); same-window metric values can mature (2663→2668 rows, 35748→36070 impressions) because GSC data is live/lagged — reproducibility is about deterministic window selection, idempotent upsert of the current key space, and truthful status, not frozen values; max end_date 2026-09-16 is a window label, not proof every day inside it is finalized. Historical note: this row was accepted before the later P1 closures. As of 2026-09-19 all current P1 rows are PASS; overall SEO parity remains incomplete because P2-P13 remain PENDING. |
| P1 | Priority index/index-coverage evidence is operational | PASS | Branch `seo/parity-p1-index-coverage-20260916` (base `56690c702801c0f20d1ceebd8bb7ccb67950423a`): the accepted five-file P1B implementation is committed as `84bfb40a435389c6e8bde5749d49b3272d3bea78` with message `feat(seo): make index coverage measurement truthful`; candidate-scoped prior-history reads are greedily chunked under `INSPECTION_FILTER_BUDGET_BYTES=2000` instead of one unbounded PostgREST `.in` query, and no global `limit(5000)` remains. The 5100-candidate regression reads exact history in ≤200 chunks (actual ~165), preserves never-inspected-first then oldest-inspected ordering, and cannot pass under a one-URL-per-chunk regression (5100 chunks); no candidate is misclassified by global 5000-row truncation. History-read errors fail closed before GSC URL Inspection and record `selectionStrategy='unavailable'`, `priorHistoryAvailable=false`, and truthful actual `priorHistoryReads` (attempted reads; 0 if client creation throws). `maxUrls` validation rejects explicit null/string/zero/negative/fractional before any inventory/inspection/metadata side effects, missing `maxUrls` defaults to 50, and values >50 clamp to 50. Successful observations, including indexed URLs, persist; partial/failure semantics and run metadata `kind='manual'` plus `summary.measurementKind='index-coverage'` remain (no schema migration; the production migration and its `seo_engine_runs` / `gsc_index_coverage` tables already exist and are protected). Lightweight estate inventory remains tree-only (no page blobs). Subrequest note: history chunk reads add to the ≤50 URL inspections; the 5100 stress fixture is ~165 history reads plus inventory/inspection overhead and stays under the repo-configured `wrangler.toml` subrequests=5000, superseding the earlier understated total. Fresh supervisor evidence (2026-09-17, local): `git diff --check` exit 0; focused suites `tests/gsc-index-coverage-route-truth.test.ts` + `tests/gsc-index-coverage-truth.test.ts` + `tests/site-health-inventory.test.ts` 3 suites / 30 tests pass; `npx tsc --noEmit` exit 0; full Jest 405 suites passed / 2 skipped (4304 tests passed / 4 skipped) exit 0; `npm run build` exit 0 with Next.js compile + TypeScript + OpenNext Cloudflare bundle and `.open-next/worker.js` generated; post-build generated SEO data diffs were restored, leaving exactly five intended modified files (`app/api/content-studio/gsc/index-coverage/route.ts`, `docs/superpowers/seo-parity-matrix.md`, `lib/gscIndexCoverage.ts`, `tests/gsc-index-coverage-route-truth.test.ts`, `tests/gsc-index-coverage-truth.test.ts`). Two independent read-only DeepSeek reviews found no material blockers after the bounded-history fix. Caveat: results are a bounded max-50 URL Inspection sample, never estate-wide zero. Production completion proof (2026-09-17): PR #218 merged the truthful index-coverage implementation to `main` as merge commit `39040b8500959a73c3a7922c2325e77f37690ffa`. PR #219 mounted the admin Search Console/Indexing UI and fixed property-query truth; the exact reviewed head `f4fb9e65ba19a5a13d4b38f5e2d5f28a5e75c640` merged to `main` as `3f896ae727801e40816b62cd91cd69e2373ebcef`. PR-head workflows on #219: `Content Studio Review` success; `Deploy YouSafe Portal` PR-context checks passed typecheck, unit tests, build, and SEO audit. Main push `Deploy YouSafe Portal` run #2889 (run id `35240269227`) for merge SHA `3f896ae727801e40816b62cd91cd69e2373ebcef` completed success including typecheck, unit tests, Next/OpenNext build, SEO audit, Cloudflare deploy, secrets sync/health, and post-deploy smoke. Authenticated production URL `/dashboard/admin/gsc` rendered Search Console, service-account connected, Indexing tab, cached list data (the cached list path showed prior `CRAWLED_NOT_INDEXED` evidence), and the Scan index coverage control. Live scan completion UI: "URL Inspection sample partial: 45/50 successful · 28 actionable issues · 5 failed. Not an estate-wide coverage total." Supabase `seo_engine_runs` row id `ec8c451e-01cf-49fe-9eff-96cba119ab28`: status `partial`, `measurementKind=index-coverage`, `measurementState=partial`, `scope=all`, `maxUrls=50`, `candidateCount=528`, `requestedUrlCount=50`, `attemptedUrlCount=50`, `inspectedUrlCount=45`, `failedCount=5`, `cachePersisted=true`, `priorHistoryAvailable=true`, `priorHistoryRows=1`, `priorHistoryReads=23`, `selectionStrategy=never-inspected-first_then_oldest-inspected`, `siteUrl=sc-domain:yousafeconsultancy.com`; started `2026-09-17 15:42:26.408+00` and finished `2026-09-17 15:44:39+00`. `gsc_index_coverage` after the scan: 46 rows total, 17 indexed, 29 non-indexed, `latest_inspected_at` `2026-09-17 15:44:39+00`. This proves a truthful bounded max-50 sample, partial failure semantics, cache persistence of successful indexed + non-indexed observations, and run metadata; it does not prove estate-wide coverage or 100% success. PASS here means only that the operational bounded-sample index-coverage requirement is proven. As of 2026-09-19 all current P1 rows are PASS; overall SEO parity remains RED because P2-P13 remain PENDING. |
| P1 | Qualified visibility separated from raw off-mission visibility | PASS | Production acceptance 2026-09-19. Implementation landed through PR #224 (`d5348fd9`, base four-bucket visibility + full-window summary/UI/action boundaries), followed by production-proof hardening PR #225 (`f80ce6be`) and edge-case hardening PR #227 (`a39c124c`). The first main deploy after #227 (run #2904 / `35424024633`) passed typecheck, unit tests, Next/OpenNext build and SEO audit but failed only at Cloudflare upload because active `wrangler.toml [limits].cpu_ms` is unsupported on the Free plan (`100328`); deployment-only PR #229 removed the paid-only limits block and merged as `217b4da50b0c05ce1a476fa953f08fb7ccf003c1`. Exact-main Deploy YouSafe Portal run #2907 (`35424902862`) then completed success through typecheck, unit tests, build, SEO audit, secrets preparation/sync, Cloudflare deploy, secrets-health verification, and post-deploy smoke. Authenticated production `/api/content-studio/gsc/performance?days=90&limit=40` on that deployed main reports `ok=true`, requested range `2026-06-21→2026-09-18`, truthful latest-window fallback `usedFallback=true` to persisted range `2026-06-20→2026-09-17`, and full-window scope `2663/2663` scanned/persisted, `countKnown=true`, `unscannedRows=0`, `complete=true`, `truncated=false`, `rowMismatch=false`, cap 25,000. Raw visibility is 36,041 impressions / 2 clicks; qualified visibility is 2,292 impressions across 140 rows (6.4% of raw; 0 clicks); off-mission visibility is 617 impressions across 135 rows (1.7%; real demand, non-actionable); junk is 30,775 impressions / 1,242 rows; deep-tail is 2,357 impressions / 1,146 rows. Authenticated Content Studio Discover renders `Visibility mix · full persisted window · 90d`, `Complete measurement`, `Raw visibility 36,041`, `Qualified visibility 2,292 · 6.4% of raw impressions · 0 clicks`, and `Off-mission visibility 617 · 1.7% of raw impressions · real demand, not actionable`; the drawer states `2,663 scanned of 2,663 persisted rows` and keeps the top-40 table explicitly diagnostic. Authenticated `/api/content-studio/opportunities/score?days=90&limit=200` reports `ok=true`, `usedFallback=true`, `excludedNonActionable=116`, `count=84`; exact production residuals are absent from scored output: `student rentals near university of south carolina`, `student rentals near florida international university`, `student living university of south carolina`, `international student storage cornell`, `is warwick safe for international students`, `"fy27_stk_housing_rates" pacific`, `you safe`, `you safe consultancy`, `you safe contact number`, `you safe login 2024`, and `you safe ltd`. Raw rows remain observable/classified (for example `international+student+storage+cornell` appears in the performance diagnostics as `off_mission`) while only `qualified` rows drive opportunity scoring/cannibalization/brief evidence. Scope caveats: this proves the persisted 90-day query×page window and current action surfaces, not Google property-wide completeness; the API explicitly used the latest persisted fallback window rather than pretending the requested newer window existed. Historical note: those later P1 rows have since passed. As of 2026-09-19 all current P1 rows are PASS; overall SEO parity remains incomplete because P2-P13 remain PENDING. See `docs/superpowers/seo-execution-ledger.md` → 2026-09-19 qualified-visibility production acceptance. |
| P1 | LLM audit failures excluded from genuine citation-loss math | PASS | Production acceptance 2026-09-19. PR #231 head `6fa4732c032ce48cf56b6532d05325cfcaabcf19` merged to `main` as `9c5db317341f512d9d68854d727cf7bc54fde256`; exact-main Deploy YouSafe Portal run #2911 (id `35429172831`) completed success through typecheck, unit tests, Next/OpenNext build, SEO audit, Cloudflare deployment, secrets-health verification, and post-deploy smoke. Read-only production SQL over `seo_llm_visibility` shows 267 prompt attempts (`fan_out=false`), of which 197 are measured after excluding `audit_failed`, 0 measured rows are cited, and 70 prompt attempts are flagged `audit_failed`; all-row totals likewise show 197 measured / 70 failed. Authenticated deployed `GET /api/seo-engine/status` returned `authMode=service-role` and `llmVisibility={total:197,cited:0,shareOfVoice:0,measurementState:"measured"}`; Content Studio rendered `LLM CITED 0/197`. Failed attempts remain visible operationally (latest daily summary records `llmAudits=6`, `llmFailed=6`) while no longer entering citation denominators. Caveat: historical failed rows can still persist `cited=false` and stored `share_of_voice=0.000`; this PASS proves read/calculation semantics exclude failed audits, not a retroactive rewrite of legacy stored numeric fields. The current measured citation share is genuinely 0/197, so this PASS does not imply healthy LLM visibility. Historical note: the reward/forecast row has since passed. As of 2026-09-19 all current P1 rows are PASS; overall SEO parity remains incomplete because P2-P13 remain PENDING. See `docs/superpowers/seo-execution-ledger.md` → 2026-09-19 LLM-audit failure accounting production acceptance. |
| P1 | Reward/forecast inputs are tied to real observations | PASS | Local implementation acceptance 2026-09-19 on `seo/p1-reward-observation-truth-20260919` (base `600a0b81fa164c8f980c9f05833763d0273b4c8b`): forecast-vs-actual evaluation is diagnostic-only and persists zero forecast reward rows; manual reward POSTs are forced audit-only (`manual_unverified`, reward 0, no recalibration); model training accepts only verified intervention-bound `cron_gsc_improvement` rows with real HTTP(S) URL, exact query, completed window, measured baseline, known action, deterministic `cron-attr:` identity, positive measured click delta/reward and attributed family credit. Calibration reads fail closed, active lineage requires an observed-reward note with a persisted `through <observed_at>` watermark, delayed/backfilled evidence is not skipped, eligible evidence is bounded-paginated with overflow fail-closed, and legacy forecast/manual calibration rows are excluded from active weights/reporting. Initial evidence columns came from `20260903_seo_engine_integrity.sql` and `20260907_engine_evidence_attribution.sql`; production bulk-upsert acceptance later exposed that the original partial dedupe index could not back Postgres `ON CONFLICT (dedupe_key)`, so PR #236 added and deployed `20260919165400_reward_dedupe_full_unique.sql`, replacing it with a normal UNIQUE index while preserving multiple NULL audit keys. Local proof: focused 10 suites / 92 tests PASS; `npx --no-install tsc --noEmit` PASS; `git diff --check` PASS; full `TZ=UTC NODE_OPTIONS=--max-old-space-size=8192 npx --no-install jest --ci --runInBand` = 425 suites passed / 2 skipped, 4492 tests passed / 4 skipped, exit 0; `npm run build` PASS through Next 16.2.11 + OpenNext Cloudflare with `.open-next/worker.js`. Production acceptance 2026-09-19: implementation PR #233 merged as `a42c510ec65bcea61c68d6e1d6d63b6d4bec155b`; runtime-bound PR #234 merged as `60f2b0a382598555c87277ea255291dab43c7474`; batch reconciliation PR #235 merged as `7579d27c86dca29bbdd1cbee40161d012bf9daaa`; dedupe-schema PR #236 merged as `d13041148e82209758b4eb32f7b67b00aa525928`. Main migration run `35456821844` succeeded and the migration ledger records `20260919165400_reward_dedupe_full_unique.sql` applied by `ci-runner` from `d1304114…` at `2026-09-19T17:02:38.065895+00:00`; live `uq_reward_events_dedupe` is a non-partial UNIQUE index on `dedupe_key`. Exact-main deploy run `35456821822` succeeded through typecheck, unit tests, Next/OpenNext build, Cloudflare deploy, Worker-secret health and post-deploy smoke. Production reward run recorded at `2026-09-19T17:08:34.929962+00:00` completed `success` with `jobsConsidered=223`, `preparedEvents=724`, `distinctWindows=26`, `events=724`, `persistFailed=0`; all 724 rows are real HTTP(S) page/query/window `cron_gsc_observation` records with deterministic `cron-attr:` identities, `improvement_credited=false`, reward 0 and no positive click delta; 530 carry measured baselines and 679 carry known actions, while rows lacking baseline/action remain non-training. A second exact-main workflow run `35457162370` completed in ~4s with `events=0`, `historyRows=724`, `duplicatesSkipped=724`, proving idempotent replay with no writes. Final production ledger = 735 rows: 724 real non-credit GSC observations + 11 historical forecast audit rows; 0 `cron_gsc_improvement`, 0 manual rows, and no new positive rewards. Authenticated `/api/seo-engine/rewards?limit=40` reports `training.eligibleEvents=0`, `trainingEligibleEvents=0`, default weights and `activeCalibration=null`; authenticated `/api/content-studio/model-calibration` reports `lastCalibratedAt=null`, `eventsCount=0`, so the two legacy forecast calibration rows remain audit history only and are not active. Post-observation weekly run `35457318418` returned `ok=true`, `events=0`, `recalibrated=false`, `weightsChanged=false`, `eligibleObservedRewards=0` and `forecast reward writes=0`; calibration table remained exactly 2 legacy forecast rows / 0 observed-reward calibrations. No synthetic manual production event was injected solely for evidence; the manual route remains pinned by focused tests to `manual_unverified`, reward 0, no recalibration, and the shared training predicate excludes it. This closes the final P1 row: all current P1 rows are PASS; overall SEO parity remains incomplete because P2-P13 remain PENDING. |
| P2 | Sitemap contains zero invalid public URLs | PASS | Production acceptance 2026-09-20: low-concurrency crawl of all seven public sitemap roots (apex, Market, Legal, USA, CA, UK, AU) returned 1,744 entries = 1,744 unique URLs, rootErrors=0, redirectCount=0, invalidFinalCount=0 (`/tmp/yousafe-p2-low-concurrency-audit.json`, at `2026-09-20T04:25:10.833Z`). Current sitemap parameter check also reports 1,744 total / 1,744 unique / 0 parameterized URLs. Release chain includes Market static-estate restoration PR #246 → `665c6af1`, Caseworks redirect authority/restoration PR #151 → `0460210`, and apex OpenNext static-cache CPU fix PR #188 → `8dfd23ea`; their exact-main deploys passed. |
| P2 | Meaningful canonical conflicts = 0 | PASS | Canonical audit before the final repair isolated exactly one meaningful conflict: Legal `/us/personal-statement/` redirected to a missing apex winner and ended 404/noindex/canonical-mismatch; the only missing-canonical item was plain-text `llms-full.txt`, not HTML. Caseworks PR #151 removed the bad redirect and restored `/us/personal-statement/`, `/us/admissions-consultant/`, and `/us/research-proposal/`; live acceptance after deploy proved all three HTTP 200, self-canonical, `index,follow`. The six intended consolidation redirects remain redirects but are excluded from the Legal sitemap by the shared effective-redirect authority. Final seven-root sitemap crawl reports 0 redirected sitemap pages / 0 invalid finals. |
| P2 | Redirect/4xx/parameter duplicates/broken internal links reconciled | PASS | Production reconciliation 2026-09-20: Caseworks PR #151 unified Worker `REDIRECT_MAP`, dual-graph and active `public/_redirects` authority; post-build drift = 421 expected / 421 emitted / 0 redirect sources; redirect shadowing = 0 and dead redirect targets = 0; redirect intent safety = 304 definitions PASS. The earlier rendered-link audit slow recheck isolated three durable 404s (`/us/admissions-consultant`, `/us/personal-statement`, `/us/research-proposal`), all fixed by #151. Its only two transient Market search-query 503s now return 200 individually. Current public sitemaps contain 0 query-string URLs, 0 duplicates, 0 redirects, and 0 non-2xx finals. Apex PR #188 removed the Cloudflare 1102/`sitemap.xml` 500 failure by serving the prerender cache from static assets; production smoke and five independent rounds returned 200 for homepage, sitemap, representative blog and guide routes. |
| P2 | Priority indexable orphans resolved or justified | PASS | Rendered estate crawl found 164 zero-inbound sitemap pages before repair. Priority was derived from the existing program boundary, not an invented impression threshold: persisted GSC rows classified by `classifyPersistedGscRow` produced exactly 4 qualified orphan pages (dependent-visa UK, F-1 health insurance, STEM MBA, Masters Data Science CA); ownership-registry owner/supporting URL intersection added exactly 3 distinct guide destinations (Day-1 CPT vs regular CPT, MBA vs MiM, OPT application). Caseworks PR #152 made closed article clusters server-rendered/crawlable; scope-correction PR #153 retained that fix and removed non-priority registry expansion. Live `/articles/` HTML after #153 contains real anchors for all 7 priority URLs. Legal-only rendered crawl after deploy: 421 pages, 0 fetch errors, exactly 1 residual orphan (`/ca/study-permit-refusal-reapply/`); it is justified non-priority because it has no ownership-registry role and zero rows/impressions/clicks in the persisted 2026-06-20→2026-09-17 GSC window. |
| P3 | 100% strategic intents have one owner | PASS | Production acceptance 2026-09-20: source-of-truth registry PR #1 in `yousafe-seo-strategies` merged as `dad9e2251a36422e159a834c927d739afadb0b58`; Portal PR #248 exact head `9035d97091d0edd28d0c16d1cabc5781eb82f8bd` merged to `main` as `2aa6a67c5cd7d59c4f2cf7bd354452cf99441874`. Exact-main `Deploy YouSafe Portal` run #2945 (`35496234655`) completed SUCCESS through TypeScript, unit tests, Next/OpenNext build, SEO audit, Cloudflare deploy, Worker-secret health, and post-deploy smoke. Registry copies are byte-identical at 76 rows and all 76 strategic rows are confirmed/mapped. |
| P3 | ≥95% active priority intents mapped | PASS | Production acceptance 2026-09-20: 76/76 strategic rows mapped = 100%. Live ratification corrected stale university/housing owners to indexed regional pages and specific F-1 blog ownership. Five mapped rows remain deliberately non-authorable by action: 3 `build` + 2 `supply_first`; row 39 is a family namespace requiring a concrete leaf and row 65 targets a currently 404 future leaf. Marketplace supply proof found 4 F-1-specific gigs and 23 study-permit gigs. Exact-main deploy `35496234655` succeeded. |
| P3 | CREATE is blocked without owner resolution | PASS | Production acceptance 2026-09-20: shared `isAuthoritativeOwnershipRow` requires `confirmed` + action in `{keep,expand,merge}` + HTTPS known-estate owner + non-generic canonical; generic roots and `build`/`supply_first` remain fail-closed. PR #248 passed Content Studio Review #153 (`35496000625`) and PR deploy checks #2944 (`35496000575`), then merged as `2aa6a67c5cd7d59c4f2cf7bd354452cf99441874`; exact-main deploy #2945 (`35496234655`) succeeded including Cloudflare deployment, Worker-secret health, and post-deploy smoke. Ratified focused gate: 23 suites / 259 tests PASS; TypeScript and `git diff --check` PASS. Broad net-new CREATE remains frozen until P13. |
| P4 | Major priority-cluster cannibalization resolved | PASS | Production acceptance 2026-09-20. Spec gate: no unresolved major collision in the five priority clusters; redirect/noindex actions carry evidence and regression coverage (`docs/superpowers/specs/2026-09-15-seo-cleanup-expansion-parity-design.md` P4). Release chain: implementation PR #251 merged as `a26501f73a69212e6233dd4f38f8a0106522f353`; post-P4 trigger-search-path PR #252 merged as `77105d511552dd64439aa6dcfcf70a6d8f395324`. Exact-main `Deploy YouSafe Portal` run `35510937704` SUCCESS and exact-main `Apply SEO Factory Migrations` run `35510937689` SUCCESS, both at `77105d51`. Production acceptance: `seo_cannibal_decisions` exists, the append-only trigger `seo_cannibal_decisions_append_only` exists with pinned empty `search_path`, and the ledger holds 0 rows. Evidence method/window: persisted qualified GSC only (`sc-domain:yousafeconsultancy.com`, 2026-06-22 → 2026-09-19, 2,660 rows, last sync 2026-09-20 05:39 UTC); no live GSC API pull was made, so live-query coverage remains UNKNOWN. Per-family dispositions: Canada spouse/spousal sponsorship has two qualified historical overlaps (`canada spouse visa`, `2024 canada spouse visa`) occurring only on legacy losers, and both legacy URLs HTTP 301 to the row-66 owner on `legal.yousafeconsultancy.com` (`/ca/family/canada-spousal-sponsorship-document-checklist-2026/`) ending 200 — no actionable competing pair remains; US F-1/CPT/OPT/STEM OPT, Australia 485, and Express Entry have no actionable qualified overlap, with subtype/ownership boundaries fail-closed; UK Student/Graduate/Skilled Worker/dependants has one qualified historical Warwick overlap only (Legal guide vs UK university hub) whose Legal guide HTTP 301s to `https://uk.yousafeconsultancy.com/universities/university-of-warwick/` and ends 200, with no actionable route-family overlap. Truthful summary: three exact qualified historical overlaps exist, all on already-redirected loser URLs; ZERO actionable currently competing pairs remain. Legacy `cannibal_merges` is not P4 authority; `seo_cannibal_decisions` is the authoritative append-only P4 ledger. P3 ownership-hygiene gaps (Australia 485 rows 59/76; Express Entry CRS/draw/STEM-category) stay fail-closed and do not reopen P4 absent current qualified competition. Broad net-new CREATE remains frozen until P13. |
| P5 | High-impression off-mission URLs have explicit disposition | IN_PROGRESS | Implementation checkpoint 2026-09-20 on `seo/p5-off-mission-cleanup-20260920` (base `d5688e9f7144d7ae34526db9f6a18dd88d292d32`), **not merged and not production-verified**. Durable versioned contract added: `data/seo/p5-off-mission-dispositions.json` (`p5-off-mission-dispositions-v1`, mirrored byte-identical into `public/seo-data/`) + typed helper `lib/seoFactory/p5OffMissionDispositions.ts` using only the existing strategic vocabulary `KEEP | KEEP_BUT_SILO | MOVE | MERGE_301 | NOINDEX | RETIRE`. High-impression is defined as the smallest descending URL cohort whose cumulative off-mission impressions reach ≥75% of all off-mission impressions including boundary ties; for `2026-06-22 → 2026-09-19` that cohort is the eight `legal.yousafeconsultancy.com/guide/<slug>/student-housing/` URLs (South Carolina, FIU, Portland State, Cornell, Utah, Arizona State, Oregon, Howard), all `KEEP_BUT_SILO`. Exact evidence recorded: persisted GSC window `2026-06-22 → 2026-09-19` (2,660 rows re-verified read-only; last sync `2026-09-20 05:39 UTC`), off-mission totals `613 impressions / 134 rows / 37 URLs / 0 clicks`; per-URL off-mission split and live status/sitemap/robots/ownership observations remain explicit `null`/**UNKNOWN** with re-derivation provenance (never fabricated as 0). No redirect, noindex, canonical, robots, retirement or internal-link change is implemented or authorized for any of the eight pages. Local evidence: Node smoke harness over the real modules 23 checks PASS (registry validation, deterministic normalization, UNKNOWN-never-0, mutation verdicts, unregistered-URL pass-through, data/public byte identity, classification fixtures); `node --check` on every changed file; `git diff --check` clean. Not yet proven: the three new Jest suites and `tsc` could not run locally (this worktree's `node_modules` resolves to an empty/inaccessible directory — no install or manifest change was attempted), so they must pass in CI before any PASS claim. |
| P5 | Off-mission pages cannot generate missions | IN_PROGRESS | Implementation checkpoint 2026-09-20 (**not merged / not production-verified**). `lib/seoFactory/keywordPlanner.ts` no longer admits GSC terms to the research board (or the editorial plan built from it) on junk-only filtering: board admission now requires the metric-aware `isQualifiedGscDemandQuery()` boundary, while the explicit `includeBrand` opt-in is preserved verbatim. Auto-run Master Planner top-up (`app/api/seo-factory/auto-run/route.ts`) replaces junk-only admission with the shared metric-free `isActionableDemandQuery()` boundary (cluster-plan rows carry no GSC row metrics); the stream route's keyword-plan fill (`app/api/seo-factory/auto-run-stream/route.ts`) applies the same boundary as fail-closed defense in depth. The final pipeline backstop (`isJunkTopic`) and the broad net-new CREATE freeze are unchanged — broad net-new CREATE remains frozen until P13. New regression suites: `tests/p5-keyword-planner-demand-boundary.test.ts` (off-mission campus query cannot reach board/plan; qualified immigration/admissions/tenancy demand still can; includeBrand preserved; deep-tail excluded) and `tests/p5-auto-run-demand-boundary.test.ts` (drives the real route handlers; off-mission cluster plan and off-mission plan term cannot become candidates). Local evidence: Node smoke harness confirms every fixture is the real boundary case (23 checks PASS) and `node --check` parses all changed files; Jest/`tsc` could not run locally (missing dependencies), so CI remains the gate. Deliberate documented consequence: the locked strike-seed keyword `university of the pacific student housing` is off-mission-classified and therefore no longer enters GSC-driven planning (strike-seed routing for admitted terms is unchanged). Residual: `lib/seoEngine/planner.ts` can still persist an off-mission-titled `seo_cluster_plans` row for diagnostic surfaces; auto-run admission refuses it. |
| P6 | Approved internal-link backlog is verified live or rejected/stale | PENDING | Target ≥80%; DB `applied` alone is not proof. |
| P7 | Near-win ranking interventions have before/after evidence | PENDING | Phase not started. |
| P8 | Trust/E-E-A-T surfaces are truthful and source-fresh | PENDING | Phase not started. |
| P9 | External authority operations record verified wins | PENDING | Phase not started. |
| P10 | Conversion attribution is instrumented and truthful | PENDING | Unknown conversion data must remain unknown, never inferred. |
| P11 | GEO/AI visibility evidence is current and failure-aware | PENDING | Phase not started. |
| P12 | Full estate validation gates pass | PENDING | Phase not started. |
| P13 | Controlled expansion passes evidence/content/ownership gates | PENDING | Phase not started. |

## Status discipline

- `PASS`: direct reproducible evidence exists and remains current for the phase decision.
- `ACCEPTED_EXCEPTION`: deliberate exception with owner, rationale, and bounded risk.
- `BLOCKED_EXTERNAL`: required provider/data/system is unavailable; evidence proves the dependency failure.
- `FAIL`: evidence contradicts the target contract.
- `PENDING`/`IN_PROGRESS`: cannot advance the phase.

Every implementation PR updates this matrix before handoff to GPT-5.6 Sol.
