# SEO Parity P6 — Internal Authority Execution (foundation checkpoint)

> **Implementation record — P6 is IN_PROGRESS, never PASS.** This document records the P6 *foundation repair* only. Nothing here authorizes or performs a production/backlog mutation: no migration was applied, no `seo_interlinks` row was changed, no reconciliation/rejection/retarget ran, and broad net-new CREATE stays frozen until P13.

> **Post-merge truth (2026-09-21, appended — supersedes the pre-merge claims below).** The foundation work was pushed, reviewed, passed required checks and merged: **PR #256 landed on `main` as `ab6305a7c5c362f01b46947e53be0e462772c88e`** (base/HEAD of the reconciliation worktree). Official `Apply SEO Factory Migrations` workflow run **`35556091287`** applied `20260920130000_seo_interlinks_verification_truth.sql` successfully, and `Deploy YouSafe Portal` run **`35556091282`** is green. Every earlier "authored, NOT applied" / "PR CI has not been run / branch not pushed / Jest and tsc unproven everywhere" statement in this document describes the pre-merge checkpoints only. **P6 remains IN_PROGRESS**, P5 PASS, P7+ PENDING, CREATE frozen. The Batch A stale-rejection tool is authored and locally verified but has **NOT been executed**: no `seo_interlinks` row was changed by this checkpoint.

**Goal:** make interlink `applied` mean "verified live", stop new dead targets at persistence, and make every automatic draft link fail closed — before any backlog reconciliation is authorized.

**Design source:** `docs/superpowers/specs/2026-09-15-seo-cleanup-expansion-parity-design.md` (§ P6 — Internal authority execution).

**Branch:** `seo/p6-internal-authority-20260920` (base `e44fa01944423babe906c45f36c06bf4b6a8b4d4`).

## Authoritative audit facts (read-only, 2026-09-20)

- **1,899** persisted `seo_interlinks` rows: all `planned`; **0 applied, 0 rejected, 0 gated**.
- **206 sources / 236 targets.**
- **1,643 rows** point at real 404 targets; **183 rows** point at legacy Portal auth-wall targets; only **73 rows** have a current canonical live target (**3 Marketplace category URLs**).
- `source_slug` is a **planner identity, not a URL**. `content_jobs` has no `cluster_id`, and exact `source_slug` → `content_jobs.slug` resolution finds only **2** sources. Source URL/live proof therefore had to become durable before *any* lifecycle mutation.
- The daily planner still persisted synthetic/dead targets (`generateInterlinkPlan` templates journey/cross-country/`seo-*` targets without liveness proof).
- `ship.ts` wrote `status=applied` from an in-memory body substring immediately after commit/merge, before production live verification.

**The raw 1,899 is NOT the approved-useful denominator.** The P6 gate (≥80% of approved useful backlog verified applied or explicitly rejected/stale) remains **unevaluable** at this checkpoint: 0 verified applied.

## Foundation repair implemented (this checkpoint)

### A. Verification truth is a schema contract

`supabase/migrations/20260920130000_seo_interlinks_verification_truth.sql` (authored, **not applied**):

- additive nullable columns `source_url text`, `source_job_id uuid` (the exact `content_jobs.id` ship job that staged the edge), `verification_state text`, `verified_at timestamptz`, `verification_evidence jsonb`;
- closed vocabulary CHECK (`present | absent | source_not_live | target_not_live | unverifiable`, plus NULL = "no verdict yet");
- **validated fail-closed constraint**: `status='applied'` requires nonblank `source_url`, `verification_state='present'`, non-null `verified_at`, `verification_evidence`, and `applied_at` (production applied count is 0, so no existing row is touched);
- two justified partial indexes (`source_url`, `verification_state`), `notify pgrst, 'reload schema'`, idempotent/re-runnable DDL, and **no row reconciliation/backfill of any kind**.

### B. No pre-deploy `applied` claim

`lib/seoFactory/interlinkVerification.ts` `stageEngineInterlinksForVerification()` replaces the old ship-time applied writer. After a successful direct-main write or merge, a planned row whose exact target URL is **structurally embedded** in the shipped draft (markdown link / HTML anchor only) records `source_url = <plan canonicalUrl>` plus the exact `source_job_id = <ship jobId>` when the ship has one, and stays `planned` (`applied_at` null, no `present` verdict). Planner slugs locate candidate rows; the canonicalUrl is the only source identity, the job id is never guessed from the slug/canonical, and a different durable source identity is never overwritten. Legacy/admin staging without a job id records `source_url` only; such rows carry no job identity and are therefore never auto-finalized (they remain unresolved/manual).

**Lifecycle ordering is load-bearing:** both ship success paths `await` staging **first** and only then launch live verification (`ship.ts` line order: `stageEngineInterlinksForVerification` → `verifyLiveInBackground`). Launching verification first would race the staging write, and a verifier that finished before the rows existed could never finalize them. This ship-time call is best-effort only — the durable post-deploy opportunity is the scheduled reconciliation pass in section C2.

### C. `applied` only after `verifyLiveUrl` ok=true + exact live proof

Finalization runs only when `verifyLiveUrl(input).ok === true` for that exact `canonicalUrl`. Three callers reach it: `POST /api/content-studio/verify-published` (admin, explicit — now job-bound when the caller supplies the exact jobId); the **ship-time background path** — `verifyLiveInBackground()` → `runBackgroundLiveVerification()`, which finalizes for `input.canonicalUrl` when, and only when, the verification it just ran resolved `ok === true`; and the **durable scheduled reconciliation pass** (`lib/seoFactory/interlinkReconciliation.ts`, section C2), which is the post-deploy opportunity of record and additionally requires positive official deployment-lineage proof (`lineageVerified === true` + `publicationPhase === 'live_verified'`) — exact job identity is necessary but not sufficient there. A finalization failure is logged in isolation and can never weaken or fail a successful content verification:

- exact, normalized `<a href>` match against the LIVE source HTML (`live_exact_href`) — never plain text, `<script>` or JSON substring; trailing-slash tolerant, query/fragment strict;
- structurally safe reading: HTML comments, CDATA, `<script>`/`<style>`/`<template>`/`<textarea>`/`<title>` payloads and markup serialized inside attribute/JSON strings are stripped by a tokenizer before any anchor is read; root-relative/same-site hrefs are resolved against the verified source canonical and refused when they would leave that host;
- the target must also be live via the existing link-validity authority (`verifyUrlsLive` + `classifyLiveStatus`);
- `present` → atomic update to `status='applied'` + `applied_at` + `source_url` + `verification_state='present'` + `verified_at` + JSON evidence (source, target, proof kind, observed href, bounded source context, HTTP observations);
- live source but href missing → `absent` (stays planned); target not live → `target_not_live` (stays planned); source failure → `source_not_live` / `unverifiable`, never applied;
- idempotent: only `status='planned'` rows are selected or updated, so a transient failure can never downgrade a verified applied row;
- every write returns its **actual affected-row count** and any DB error: a zero-match concurrency race is counted as skipped (never as applied/staged/verdict truth) and a failed write is reported in `dbErrors` + `error` instead of looking like a benign no-op.

### C2. Durable post-deploy re-verification (shipping is not the only opportunity)

The ship-time background call is best-effort by construction: it runs in the same serverless task that just wrote Git, usually before the production deployment is observable, and not at all if the invocation is dropped. The durable seam is therefore a bounded server-side reconciliation pass wired to the **existing** scheduled lifecycle surface — `POST /api/cron/seo-engine-daily` (CRON_SECRET only, driven by `.github/workflows/seo-engine-daily.yml`), as its own `phase: 'interlinks'` and inside the daily `phase: 'all'`:

- `lib/seoFactory/interlinkReconciliation.ts` reads at most 200 `status='planned'` rows that already carry a durable `source_url` **and** a non-null `source_job_id` (`source_job_id IS NOT NULL` filtered in SQL, so neither the unstaged backlog nor jobless historical rows can starve/occupy the bounded window; non-UUID values are still skipped/counted server-side as unresolved);
- a source is attempted only after a 30-minute deployment-lag window and outside a 20-hour cooldown, and at most **3 sources per run** are verified (`INTERLINK_RECONCILE_*` env-tunable, finite-number guarded so a NaN can never silently disable the pass) — no broad production mutation, only staged planned interlinks are verified. Revision membership is decided by the durable nullable `staged_at` stamp written by staging/rebind — **not** by `updated_at`, which the BEFORE UPDATE trigger always pushes to `now()` and which therefore made the cooldown structurally dead; `updated_at` remains only the documented fallback for legacy already-job-bound rows staged before `staged_at` existed. A newly rebound/re-staged job therefore makes old verdict/attempt timestamps immediately stale;
- **job-bound identity:** sources are grouped by the exact `(source_url, source_job_id)` pair, and `verifyLiveUrl` is called with BOTH the exact staged `canonicalUrl` and the exact `jobId`. That is what puts a contracted job on the official deployment-lineage path (`verifyLiveUrl` → `reconcilePublicationDeployment(jobId)`) instead of the legacy/uncontracted path; a row missing or carrying an invalid job id is skipped as unresolved and surfaced truthfully (`skippedMissingJobIdentity`), and scheduled auto-finalization never falls back to legacy verification;
- `verifyLiveUrl` ok=true is necessary but NOT sufficient: finalization additionally requires `isDeploymentProvenLiveResult()` — `lineageVerified === true` AND `publicationPhase === 'live_verified'` — so an `ok=true` legacy/uncontracted health verdict (e.g. a `content_jobs` row with no `contract_id`) is surfaced as `notDeploymentProven` and creates no applied truth. Finalization runs **job-bound** (`finalizeStagedInterlinksForLiveSource({ canonicalUrl, sourceJobId })` — only rows staged by that exact job), re-proves the exact live anchor + live target, and still updates `planned` rows only;
- an `ok=false` verdict (deployment not yet observable) is benign pending truth — no phase error; a verifier throw / DB-write failure is a real error (run goes `partial`); a missing verification-truth/job column (migration not applied) is reported explicitly as `unavailable` rather than as an empty estate or a red cron.

No new route, no unauthenticated mutation surface, and no operator click is required: the daily scheduled run is the automatic re-verification opportunity, and the admin `verify-published` path remains available for immediate checks.

**Supervisor follow-up repair (this checkpoint):** the first cut of the durable reconciler called `verifyLiveUrl({ canonicalUrl })` without `jobId`, i.e. the legacy/uncontracted path, and could auto-finalize a row that carried no job identity. The seam is now exact-job-bound end to end: ship stages `source_job_id` from `opts.jobId` in both success paths; the reconciler requires a valid exact job id, verifies with `{ canonicalUrl, jobId }`, and finalizes job-bound; the ship-time background path likewise passes its exact `jobId` through. Historical/backlog rows that lack job identity (including any row staged before this repair) remain unresolved/manual and are never auto-finalized.

### D. Unguarded applied writer removed

`markInterlinkApplied` (no callers) is deleted; `recordAppliedEngineInterlinks` (loose substring → applied) is deleted. Exhaustive repository audit (`tests/p6-interlink-least-privilege.test.ts`) proves the only file that can write `status: 'applied'` is the live-proof verifier, that its patch carries the full proof contract, and that it is guarded to planned rows.

### E. Dead targets stop at persistence

`persistInterlinkPlan()` now requires live internal target proof via `filterLiveInternalUrls` **before** any Supabase client exists. Dead/synthetic targets that were checked and rejected are counted in a truthful `filtered` count and persist **zero** rows — expected P6 hygiene, **not** a fatal engine error. Only a verifier exception (unavailable) or a DB-write failure returns a real error with zero writes; no replacement URL is ever invented. `persistPlannerInterlinks()` returns `{ stored, filtered, errors }`, and the planner/cron surface `filtered` as a count while pushing only real errors (planner `persistErrors`, cron `allPhaseErrors`). Existing lifecycle/verification columns are omitted from the upsert payload, so replanning can never reset applied/gate/verification truth.

### F. Draft injection fails closed

`lib/seoFactory/interlinkInjection.ts` `pruneInterlinksToLiveTargets()` is pure and fail-closed: verifier throw or no live proof → zero links, `ok:false`. `pipelineStream` uses it for all automatic planner/radar interlinks (the old best-effort `catch` that retained unverified links is gone), reports the withheld count, and invents no replacement. `loadEngineInterlinksForCell()` additionally excludes rows whose durable verdict already proved the target/source dead (`target_not_live`, `source_not_live`) from automatic suggestions while keeping the rejected/held lifecycle guard.

### G. Read-only P6 disposition report

`scripts/p6InterlinkDisposition.ts` (+ read-only CLI `scripts/p6-interlink-disposition.mts`) classifies persisted rows using durable fields + injected live target checks into `applied_present | target_404 | legacy_auth_wall | live_target_source_verified | live_target_source_unverified | unknown`. Unknown stays unknown. `hasDurableVerificationProof` requires `source_url`, `verification_state='present'`, `verified_at`, **`verification_evidence`** and `applied_at` — the same shape the migration's fail-closed `applied` CHECK enforces. The report exposes `truncated` + `rowLimit` (`fetchP6DispositionRowsWithTruncation` proves truncation with one bounded probe row beyond the 5,000-row cap) instead of silently presenting a capped read as the complete estate. The CLI observes targets through the repository link-validity authority (`verifyUrlsLive` → `classifyLiveStatus`), so the HEAD→GET fallback for HEAD-hostile hosts and the authority-host exemptions are identical to every other link audit. SELECT-only by construction: the module has no update/delete/upsert/insert/rpc call.

**Disposition nuance (documented).** `applied_present` means the row carries durable historical proof (`source_url` + `present` + `verified_at` + `verification_evidence` + `applied_at`) — it is NOT a recheck that the page is live right now; current live state is only re-established by a later verification run. `rejected` rows are counted in their own bucket, separately from UNKNOWN/`unknown` (which stays unknown — never coerced to zero and never counted as a rejection).

### H. Least privilege (proven safe; no browser write dependency)

Repository audit: every `seo_interlinks` write is server/admin (`lib/seoEngine/interlink.ts`, `lib/seoFactory/interlinkVerification.ts`); the browser only subscribes read-only via `subscribeToTables` (pinned by `tests/p1-base-table-least-privilege.test.ts`). The migration therefore replaces the open `FOR ALL` policy with a `service_role` full-access policy plus an explicit `anon, authenticated` SELECT policy for the existing Realtime read contract, revokes all privileges from `public, anon, authenticated`, grants SELECT back to `anon, authenticated`, and grants all to `service_role`.

## Tests authored (NOT yet run — Jest/tsc unavailable locally, and PR CI has not been run for P6)

- `tests/p6-interlink-verification-db-contract.test.ts` — migration/order/contract/least-privilege SQL.
- `tests/p6-interlink-exact-href-proof.test.ts` — exact anchor proof; plain text/script/JSON rejected.
- `tests/p6-interlink-staging-vs-applied.test.ts` — staging writes `source_url` plus the exact `source_job_id` when the ship has a job id, and never applies; legacy staging without a job id writes `source_url` only (no guessed job).
- `tests/p6-interlink-finalize-live-proof.test.ts` — applied proof, absent/target_not_live/source failure, idempotency, no downgrade, verifier failure, and job-bound finalization (exact `source_job_id` filter; a different job's row and a jobless row for the same canonical are never touched).
- `tests/p6-interlink-persistence-liveness-gate.test.ts` — dead-target filtering, live Marketplace category survives, verifier-failure zero writes, replanning preserves lifecycle/verification truth.
- `tests/p6-interlink-draft-injection-fail-closed.test.ts` — fail-closed prune + pipeline source contract.
- `tests/p6-interlink-least-privilege.test.ts` — exhaustive writer audit; no unverified applied path.
- `tests/p6-interlink-disposition-report.test.ts` — classification and read-only construction.
- `tests/p6-interlink-anchor-proof-hardening.test.ts` — comment/script/style/template/serialized-payload anchors are never proof; real absolute and root-relative (same-site resolved) anchors are; cross-host, fragment and query cases; staging locators use the same rules.
- `tests/p6-interlink-durable-reconciliation.test.ts` — the scheduled reconciliation seam: exact `(source_url, source_job_id)` subjects, `verifyLiveUrl({ canonicalUrl, jobId })` and job-bound finalization, ok=true-only finalization, jobless/invalid-job rows skipped as unresolved (`skippedMissingJobIdentity`) and never auto-finalized, benign pending vs verifier-unavailable/DB errors, min-age/cooldown/bound bounds, loader failure, pre-migration `unavailable`, and no write verb/applied literal in the module.
- `tests/p6-interlink-background-finalize.test.ts` — the ship-time background path finalizes for the exact canonical and, when a job id is known, job-bound (`sourceJobId`) **and only with positive deployment lineage** (M1: an ok=true verdict without `lineageVerified`/`live_verified` finalizes nothing and logs it; the exact `live_verified` phase is required, not just the flag); legacy callers without a job id keep the documented legacy path, which is jobless-only (H1).
- `tests/p6-ship-interlink-lifecycle-order.test.ts` — both real ship success paths await staging (now carrying `jobId: opts.jobId`) before launching background verification with the same exact job id.
- `tests/p6-interlink-reconciliation-cron.test.ts` — cron `phase:'interlinks'` and `phase:'all'` wiring, benign pending/filtered truth stays green, real reconciliation/persistence failures stay `partial` with truthful phase errors.
- `tests/p6-pipeline-ship-jobid-propagation.test.ts` — both pipeline ship surfaces pass the exact persisted job id (never a synthetic `plan-*` id), and `ship.ts` binds staging + verification to `opts.jobId`.
- `tests/p6-interlink-reconcile-env-guard.test.ts` — finite-number env guard (garbage/NaN can no longer disable the pass) and the additive non-proof `verification_attempted_at` marker (exact planned tuple only, no verdict/proof column, truthful write failure).
- `tests/p6-interlink-provider-link-liveness.test.ts` — provider/profile/gig links fail closed on verifier throw / dead URLs while citation metadata survives; `verifyMarketplaceServiceUrlsLive` really calls `verifyUrlsLive` (no exemption trust); both pipeline surfaces prune before the prompt; the prompt drops the mandatory URL line when no URL verified.
- `tests/p6-interlink-admin-job-binding.test.ts` — the admin verify route passes its exact `jobId` as `sourceJobId` (legacy behavior only when no jobId exists), never finalizes on a non-ok verdict, and (M1) withholds job-bound finalization on an ok=true verdict without positive deployment lineage (`interlinksWithheld: 'deployment_lineage_not_proven'`) while still finalizing a proven job-bound verdict and the jobless legacy path.
- `tests/p6-interlink-pre-migration-observability.test.ts` — pre-migration legacy-select fallback with an explicit warning, fail-closed observable DB errors, and the additive/backfill-free/not-in-applied-constraint contract for `verification_attempted_at`.
- `tests/p6-interlink-revision-staging-stamp.test.ts` — M1 end to end against a fake DB that emulates the BEFORE UPDATE `updated_at` trigger: staging/rebind writes `staged_at`, a same-source/same-job no-op does not reset it, a current verdict/attempt still cooldowns with a trigger-later `updated_at`, and an old verdict/attempt before a new `staged_at` never suppresses the reship. Extended with M2 deterministic after-select races: observed null → a newer exact-job bind survives, observed job A rebind → a newer job B bind survives, and a jobless first stage cannot overwrite a concurrent exact-job bind (all zero-success skips).
- `tests/p6-interlink-provider-link-liveness.test.ts` (extended) — H1: the ContentSpec/playbook writer-rule/prompt author surfaces lose a dead profile/service URL while metadata survives; mixed-live keeps only proven URLs; a verifier throw never drops the author; both pipelines derive the spec author from pruned citations.
- `tests/p6-interlink-finalize-live-proof.test.ts` (extended) — M2 subject CAS filters on the applied write; a deterministic rebind race makes the old job's applied/absent verdict write a skipped zero-row CAS; L1 malformed `jobId` fails closed before any read/write; H1 legacy scope (`source_job_id IS NULL` SELECT, job-bound-only canonical → zero checked/applied with a truthful warning and no write, jobless row still finalizes, and both after-select races — source re-point and concurrent job bind — count skipped and leave the newer subject untouched).
- `tests/p6-interlink-draft-injection-fail-closed.test.ts` (extended) — L2: root-relative candidate resolves to the proven absolute URL, partial live sets report `withheld`, external candidates stay withheld, both surfaces report partial withhelds; plus the LOW observability contract: a verifier throw is `verifierUnavailable: true` while a successful verification that proved nothing live is NOT a verifier failure, slash variants of one dead target count once, and both pipeline-stream messages distinguish unavailable from nothing-live.
- `tests/p6-interlink-staging-vs-applied.test.ts` (extended) — `staged_at` written on first staging/rebind, not reset by a same-job no-op, and withheld (with an observable warning) when the additive column is unavailable.
- Updated `tests/p6-interlink-durable-reconciliation.test.ts` (staged_at revision membership + trigger-later `updated_at` cooldown regressions, legacy `updated_at` fallback), `tests/p6-interlink-verification-db-contract.test.ts` (staged_at additive/commented/out-of-constraint), `tests/p6-interlink-persistence-liveness-gate.test.ts` (replanning omits `staged_at`), `tests/p6-interlink-least-privilege.test.ts` (planned-only fence on the shared applied-write helper + CAS fields), and `tests/helpers/p6InterlinkFakeDb.ts` (trigger emulation + post-select rebind hook).
- Extended `tests/p6-interlink-durable-reconciliation.test.ts` (strict lineage gate: legacy/uncontracted `ok=true` never finalizes; fully proven verdict does; stale timestamp cannot suppress a new revision; attempt cooldown/rollover; NaN options), `tests/p6-interlink-staging-vs-applied.test.ts` (null→job rebind, oldJob→newJob rebind, sameJob no-op, jobless preserve, malformed-job refusal, different source_url untouched), `tests/p6-ship-interlink-lifecycle-order.test.ts` (merge path carries the exact job id; degraded staging is observable in `ShipResult` + a structured warning and never fails the ship).

Existing suites updated for the new contracts: `tests/interlink-lifecycle-truth.test.ts`, `tests/interlink-marketplace-persistence-guard.test.ts`, and the `persistPlannerInterlinks` mocks in `tests/engine-evidence-handoff.test.ts`, `tests/master-engine-e2e.test.ts`, `tests/p5-planner-demand-boundary.test.ts`, `tests/planner-filter-inclusion.test.ts`, `tests/seo-engine-daily-gsc-persist.test.ts`.

## Verification performed at this checkpoint

Jest/tsc are unavailable in this worktree (`node_modules` resolves to an empty directory; no install attempted). Substitute evidence:

- temporary no-dependency Node smoke harness over the real modules (only Supabase/planner/link-validity boundaries stubbed), **48/48 checks PASS**, including RED controls showing the new assertions reject the pre-P6 behaviours (substring "proof", best-effort link retention, applied-without-proof patch);
- `node scripts/migration-ledger-policy.mjs --check` exit 0 (manifest, naming policy, transaction safety);
- `node scripts/migration-order.mjs --json` places the new migration after `20260920120000_...` and before the index tier;
- `node --check` parse of every changed `.ts` file (three files need TS-annotation-free copies because Node's checker does not strip types in `--check` mode; all parse);
- exhaustive grep: no `markInterlinkApplied`, no `recordAppliedEngineInterlinks`, and the only `status: 'applied'` writer is `lib/seoFactory/interlinkVerification.ts`;
- `git diff --check` clean.

**Supervisor-review repair (autosquash `fixup!` commits — NOT amended in place).** Review rejected the first foundation commit `e9bfe607` on one material blocker: ordinary successful shipping could leave valid staged links planned indefinitely, because finalization was reachable only from the admin `verify-published` path and both ship success paths launched `verifyLiveInBackground()` before awaiting staging. Repaired in `c7f2b43d` (new `runBackgroundLiveVerification()` finalizes for the exact `input.canonicalUrl` only when the `verifyLiveUrl` it just ran resolved `ok === true`, isolated from content-verification failure; both ship success paths now await staging before launching verification; new `tests/p6-interlink-background-finalize.test.ts` and `tests/p6-ship-interlink-lifecycle-order.test.ts`) and `eb2b1133` (test-lifecycle hardening so the background-finalize negative cases cannot pass vacuously). Repair evidence: temporary no-dependency harness over the real `lib/seoFactory/liveVerify.ts` **14 PASS / 0 FAIL**, including a RED control showing pre-repair fire-and-forget semantics finalize nothing; git-proven ordering against the rejected tree (`e9bfe607` verify 853/1011 before stage 857/1013; repaired tree stage 862/1020 before verify 867/1025).

**Second supervisor-review repair (one ordinary commit, no history rewrite).** Independent review of the repaired foundation raised six findings, all addressed in the newest ordinary repair commit (no rebase/squash/amend; the previous commits above are untouched):

1. *Durable post-deploy finalization* — the ship-time background verification was the only automatic opportunity and normally ran before production was observable. Added `lib/seoFactory/interlinkReconciliation.ts` and wired it to the existing scheduled surface (`/api/cron/seo-engine-daily` `phase:'interlinks'` + inside `phase:'all'`, CRON_SECRET, `.github/workflows/seo-engine-daily.yml`), bounded (≤200 staged rows scanned, ≤3 sources verified per run, 30-min deployment lag, 20-h cooldown), planned+`source_url` rows only, `verifyLiveUrl` ok=true-only finalization for the exact staged source identity. No new route and no unauthenticated mutation surface; the immediate ship verification remains best-effort but is no longer the only automatic path; no operator click is required.
2. *Proof correctness* — `extractAnchorHrefs`/`exactAnchorHrefMatch` now tokenize the HTML and structurally skip comments, CDATA and `<script>`/`<style>`/`<template>`/`<textarea>`/`<title>` payloads (including `<a href=…>` text serialized inside an attribute string), and safely resolve root-relative/same-site hrefs against the verified source canonical while refusing any href that would leave that host. Regressions added in `tests/p6-interlink-anchor-proof-hardening.test.ts`.
3. *Planner persistence status semantics* — `persistInterlinkPlan`/`persistPlannerInterlinks` now return a truthful `filtered` count for successfully checked-and-rejected dead/synthetic targets (zero persisted, **no** error), while a verifier exception/unavailability or DB-write failure still returns a real error with zero writes. The cron surfaces `interlinksFiltered` and no longer goes red just because dead planner targets were rejected.
4. *DB write observability* — staging and verdict/applied writes now report actual affected rows (`skipped` for zero-match concurrency races) and surface DB failures in `failed`/`dbErrors` + `error`; an error is never converted into applied truth.
5. *Disposition report fidelity* — `hasDurableVerificationProof` now requires `verification_evidence` exactly as the DB constraint does; the report exposes `truncated`/`rowLimit` (proven with one bounded probe row beyond the cap) instead of implying the capped read is complete; the CLI observes targets through the repository link authority (`verifyUrlsLive` → `classifyLiveStatus`, HEAD→GET fallback) while staying SELECT-only.
6. *Docs/tests* — this plan, the ledger and the parity matrix were corrected; the "automatic at ship instant" framing is replaced by the durable scheduled seam; accepted residuals are recorded below; focused regressions were added for every repaired behaviour.

Repair evidence: the temporary no-dependency Node harness over the real changed modules (only Supabase/planner/link-validity boundaries stubbed) is **88 PASS / 0 FAIL** after this repair, covering the sanitizer/proof rules, same-site resolution, staging/finalize write observability, the durable reconciliation cadence/fail-closed/error semantics, planner filtered-vs-error semantics, and disposition evidence/truncation truth. `node scripts/migration-ledger-policy.mjs --check` exit 0; `node scripts/migration-order.mjs --json` still lists 75 entries with `20260920130000` after `20260920120000_...`; type-stripped parse check of every changed `.ts`/`.mts` file; exhaustive grep still shows exactly one `status: 'applied'` writer (`lib/seoFactory/interlinkVerification.ts`); `git diff --check` clean; no dependency install and no manifest/lockfile change.

**Third supervisor follow-up repair (one ordinary commit, no history rewrite).** Supervisor review of `403ba549` found a RELEASE BLOCKER: the new durable reconciler called `verifyLiveUrl({ canonicalUrl })` **without `jobId`**, putting verification on the legacy/uncontracted path and bypassing `reconcilePublicationDeployment` / official deployment lineage, and could auto-finalize rows that carried no job identity. Repaired:

1. *Job-bound staging* — the still-unapplied P6 migration gains nullable `source_job_id uuid` (exact `content_jobs.id`) additively/idempotently; both ship success paths pass `jobId: opts.jobId` into `stageEngineInterlinksForVerification`, which writes it alongside `source_url`. The job id is never guessed; legacy/admin staging without one records `source_url` only.
2. *Job-bound durable reconciliation* — staged rows load `source_job_id`; sources group by exact `(source_url, source_job_id)`; `verifyLiveUrl` is called with `{ canonicalUrl, jobId }` (official deployment lineage for a contracted job); only `ok === true` finalizes, and finalization passes `sourceJobId` so only that exact job's planned rows can apply. A missing/invalid job id is skipped as unresolved and surfaced truthfully (`skippedMissingJobIdentity`); the scheduled seam never falls back to legacy verification. Bounded cadence, planned-only fencing, exact href/live-target proof, idempotence and no-downgrade are unchanged.
3. *Ship-time path + DB contract + docs* — the ship-time background finalizer also threads its exact `jobId`; the applied DB constraint intentionally still requires only `source_url + present + verified_at + evidence + applied_at` (no `source_job_id` requirement), so legacy/manual applied rows stay valid; docs now state scheduled auto-finalization is job-bound and deployment-lineage verified, and that historical/backlog rows without job identity remain unresolved/manual.

Repair evidence: temporary no-dependency Node harness over the real changed modules (only Supabase/planner/link-validity boundaries stubbed) **49 PASS / 0 FAIL**, including four RED controls executing a core-logic snapshot of the rejected `403ba549` reconciler (jobless verify call, jobless finalization, a jobless row auto-finalized from `source_url`, and no unresolved-job accounting) against the GREEN repaired module, plus a real replanning run proving the upsert payload omits `source_job_id` (like every lifecycle/verification column) and the stored exact job identity survives; `node scripts/migration-ledger-policy.mjs --check` exit 0; `node scripts/migration-order.mjs --json` keeps `20260920130000` after `20260920120000_...`; type-stripped ESM parse check of all 34 changed `.ts`/`.mts` files OK; exhaustive grep still shows exactly one `status: 'applied'` writer (`lib/seoFactory/interlinkVerification.ts`); `git diff --check e44fa019..HEAD` clean; no dependency install and no manifest/lockfile change.

**Fourth independent-review repair (pre-PR; one ordinary commit, no history rewrite).** Review closed the remaining MEDIUM findings plus supervisor edge cases before the PR. Nothing about the status changed: P6 stays **IN_PROGRESS**, P5 **PASS** untouched, P7+ **PENDING**, CREATE frozen.

1. **Pipeline job-id propagation.** `pipelineStream` now passes the EXACT `earlyJobId` (the realtime `content_jobs.id` it created/claimed) into `shipContent`, and `pipeline` (non-stream) passes the EXACT `input.existingJobId` when the caller really has one — and nothing when it does not (its own job row is persisted AFTER ship, so the synthetic `plan-*` ContentSpec id is never a ship/job identity). `ship.ts` already threads `opts.jobId` into both staging and background verification; staging/verification is now bound to the real persisted job on both normal ship surfaces.
2. **Staging rebind on reship (supervisor).** `stageEngineInterlinksForVerification` selects `source_job_id` and, for the same canonical with a new nonblank VALID (UUID) exact job id: null/different → the PLANNED row is rebound to the new exact `source_job_id` (additive patch only — no status/verdict/applied column moves); same job → idempotent no-op; a jobless caller never clears/overwrites an existing `source_job_id`; a malformed job id is never written; a different durable `source_url` stays untouchable. A missing `source_job_id`/`staged_at` column (a PARTIAL migration — the pre-P6 columns, including `source_url`, still exist) falls back to the readable column set with an explicit `warning` instead of looking like a zero-candidate no-op; a TRUE pre-migration schema has NO fallback and stays fail-closed with zero writes and an explicit error (seventh repair).
3. **Strict deployment-lineage gate (supervisor).** `reconcileStagedInterlinks` auto-finalizes ONLY when `verifyLiveUrl` returned `ok === true` AND `lineageVerified === true` AND `publicationPhase === 'live_verified'` (`isDeploymentProvenLiveResult`). An `ok=true` verdict without positive official deployment-lineage proof — including the legacy/uncontracted health path taken by `content_jobs` rows with no `contract_id` — is reported truthfully as `notDeploymentProven` (count + detail) and creates NO applied truth. Exact job identity is **necessary but not sufficient**. `deployment_pending` stays benign; verifier exceptions stay real errors.
4. **Reconciliation window starvation / cooldown (MEDIUM).** The default loader filters `source_job_id IS NOT NULL` at SQL level so jobless historical rows can never occupy the bounded scan (they stay unresolved/manual; non-UUID values are still skipped/counted server-side). Cooldown only applies when the verdict/attempt timestamp belongs to the CURRENT staged revision (`>= updated_at`), so a newly rebound/re-staged revision is not suppressed by an older timestamp. Repeated non-ok attempts are bounded by the new additive nullable `verification_attempted_at` marker written for the exact planned tuple — a NON-proof column (never `verified_at`/`verification_state`/`verification_evidence`, never part of the applied constraint, never backfilled); the batch order puts never-attempted sources first and then the oldest attempt, so a permanently non-ok source can never monopolize every daily run. All `INTERLINK_RECONCILE_*` numbers go through a finite-number guard (NaN can no longer silently disable the pass).
5. **Staging failure observability (MEDIUM).** Both ship success paths now inspect the staging result, add an additive optional `interlinkStaging` summary to `ShipResult` (`staged/candidates/skipped/failed/rebounded/sourceUrl/warning/error`) and emit a structured `console.warn` when `failed > 0`, `error` or `warning` is present — without ever turning a successful content ship into a failure. A permission/RLS/network failure or a missing migration column can no longer look identical to zero candidates.
6. **Automatic draft-link fail-closed parity (MEDIUM).** The non-stream `pipeline` now prunes automatic input/planner interlinks through the same live internal-URL authority as the stream before the prompt is built, and both surfaces verify provider/profile/gig marketplace citation links through the repository's ACTUAL HTTP liveness authority (`verifyUrlsLive` + `classifyLiveStatus`) — the estate helper's `isProtectedMarketplaceUrl` sitemap exemption is never treated as proof. Verifier throw → every automatic/provider link withheld; dead links withheld; no replacement invented; author citation metadata (name/credential/role) survives, and the prompt's mandatory marketplace-URL line disappears when no URL verified.
7. **Admin job binding (LOW, closed).** `POST /api/content-studio/verify-published` passes its exact `jobId` as `sourceJobId` to `finalizeStagedInterlinksForLiveSource`, so admin verification cannot finalize rows staged by another job sharing the canonical; the legacy source-url-only behavior is preserved ONLY when no jobId exists.
8. **Pre-migration observability + env hardening (LOW).** `loadEngineInterlinksForCell` retries the legacy select (verdict unknown) with an explicit warning when `verification_state` is missing, and fails closed + observable for any other DB failure (never a fake empty cell). `.github/workflows/seo-engine-daily.yml` gains the `interlinks` `workflow_dispatch` phase choice; the scheduled `all` run is unchanged.
9. **Doc/report truth.** This plan, the ledger and the parity matrix record: exact job identity is necessary but not sufficient; scheduled auto-finalization requires positive official deployment-lineage proof; historical/jobless rows stay unresolved/manual and are excluded from the scheduled scan; `applied_present` in the disposition report is durable historical proof (not a current-live recheck) and `rejected` is counted separately from UNKNOWN.

**Fifth release-blocker repair (pre-PR; one ordinary commit, no history rewrite).** Final read-only review found five findings, all closed here. Nothing about the status changed: P6 stays **IN_PROGRESS**, P5 **PASS** untouched, P7+ **PENDING**, CREATE frozen.

1. **H1 — provider author-pack liveness bypass (release blocker).** Both pipeline surfaces pruned `providerAuthors.cited`/`links` but still passed the RAW `providerAuthors.author` into `resolveContentSpecForJob`; that pack carried `marketplaceUrl = profileUrl` (unverified) and its service pages, so an unproven marketplace URL could still reach ContentSpec → playbook/brief → writer/system prompt while the downstream audit exempts protected marketplace URLs from its dead-link check. New `authorPackFromPrunedCitations()` (in `lib/seoFactory/interlinkInjection.ts`) rebuilds the author pack from the PRUNED citation state on BOTH surfaces: name/credential/experienceScope/reviewedBy/providerType/experienceBeats survive, `marketplaceUrl` is present ONLY when the exact profile URL the pack carried is proven live, and `servicePages` are intersected with the proven-live set. A withheld URL OMITS the key (never `''`, which `validateContentSpec` rejects and which would null the whole spec). No raw unverified marketplace/profile/gig URL can remain in the writer/system prompt via author metadata, `citedProviders`, or the interlink allowlist.
2. **M1 — cooldown was dead because of the `updated_at` trigger.** The reconciler inferred the current revision with `verified_at/verification_attempted_at >= updated_at`, but the existing BEFORE UPDATE trigger always sets `updated_at = now()` later than the app timestamp, so a current verdict could never satisfy it. The still-unapplied P6 migration gains the additive nullable `staged_at timestamptz` revision stamp (idempotent, commented, unbackfilled, NOT part of the applied-proof CHECK). Staging writes it in the same planned-only update on first source/job staging and on every same-canonical rebind to a new exact job; a same-source/same-job idempotent no-op never resets it; a jobless/pre-migration fallback (source_job_id column or `staged_at` column unavailable) never invents it and says so. The reconciler loads `staged_at` and compares `verified_at`/`verification_attempted_at` against it, NOT `updated_at`; min-age uses the same revision stamp, with `updated_at` as a documented per-row fallback only for legacy already-job-bound rows without a stamp. A rebind therefore makes old verdict/attempt timestamps immediately stale. No proof field is ever fabricated.
3. **M2 — finalizer could overwrite a reship rebind.** Every job-bound verdict/applied write is now a compare-and-set on the EXACT selected subject (`id` + `status='planned'` + exact `source_job_id` + the row's exact `source_url`) in addition to the planned fence; a concurrent rebind yields zero affected rows and is counted `skipped`, never overwriting the new revision. The source-failure verdict path is fenced too. Source-only legacy/admin finalization without a job id keeps the documented legacy path, which (as of the sixth repair below, H1) is jobless-only: it CASes `source_url IS NULL`/exact observed `source_url` and can never read or finalize a job-bound row.
4. **L1 — invalid admin job id.** `finalizeStagedInterlinksForLiveSource` normalizes `sourceJobId` with the shared `normalizeSourceJobId`; a nonblank id that is not an exact `content_jobs` UUID returns an explicit fail-closed invalid-job error and never silently degrades to source-url-only legacy finalization. Omitted id keeps legacy behavior; the finalizer is the authority for the raw admin body `jobId`.
5. **L2 — interlink candidate normalization + partial withhold observability.** `pruneInterlinksToLiveTargets` accepts an optional `resolveCandidate` resolver (`resolveEstateUrl` on both surfaces) so a root-relative estate candidate matches the verifier's proven absolute URL instead of being falsely dropped; matched candidates keep their original label/`matchedOn` while emitting the proven live canonical/absolute URL. External/non-estate candidates stay withheld unless their own authority proves them, and no replacement is invented. The result exposes `withheld` even on partial success and both pipeline surfaces log/yield when it is > 0.

**Migration / replanning / docs.** The `staged_at` column is additive/idempotent/commented/unbackfilled with no backlog DML and stays outside the applied-proof CHECK; planner upserts preserve it by omission exactly like `source_job_id`/verification truth/the attempt marker (comment + regression updated). This plan, the ledger and the parity matrix record the repair truthfully, with no CI claims.

Repair evidence: temporary no-dependency Node harness over the REAL changed modules (only Supabase/planner/link-validity/liveVerify boundaries stubbed; import specifiers rewritten for Node's resolver) **39 PASS / 0 FAIL**, including RED controls (the pre-repair no-resolver prune falsely drops a root-relative candidate; the pre-repair `verified_at >= updated_at` rule fails to cool down a current verdict when the trigger stamps `updated_at` later) and execution of staging/finalization against a fake DB that emulates the BEFORE UPDATE trigger (staged_at written on staging/rebind, not reset by a same-job no-op; partial-migration retry without `staged_at`; job-bound CAS filters; rebind race counted skipped; malformed job id fail-closed; jobless legacy behavior unchanged). `node scripts/migration-ledger-policy.mjs --check` exit 0; `node scripts/migration-order.mjs --json` keeps `20260920130000_seo_interlinks_verification_truth.sql` directly after `20260920120000_...`; type-stripped `node --check` parse of every changed `.ts`/SQL file (one pre-existing typed `jest.mock` line in `tests/p6-interlink-finalize-live-proof.test.ts` cannot be parsed by `node --check`, unchanged by this repair and verified by parsing the file with that one annotation removed); exhaustive grep shows exactly one `status: 'applied'` writer (`lib/seoFactory/interlinkVerification.ts`) and only server-side `seo_interlinks` writers; `git diff --check e44fa019..HEAD` clean. No dependency install, no manifest/lockfile change, no Jest/tsc run claimed (still unavailable: linked `node_modules` resolves empty) and no CI run claimed.

**Sixth pre-push correctness repair (one ordinary commit, no history rewrite).** Final independent review found one HIGH + two MEDIUM findings plus low-risk observability gaps; all are closed here. Nothing about the status changed: P6 stays **IN_PROGRESS**, P5 **PASS** untouched, P7+ **PENDING**, CREATE frozen.

1. **H1 — legacy/no-`sourceJobId` finalization could select job-bound rows and lacked a subject CAS.** `finalizeStagedInterlinksForLiveSource` now runs two explicit, non-broadening scopes: with an exact `sourceJobId` the SELECT is `.eq('source_job_id', …)`; WITHOUT one it is `.is('source_job_id', null)`, so a source-only legacy/admin/background call never even reads a job-bound row. The result carries `scope: 'job-bound' | 'jobless-legacy'` and a truthful zero-row `warning` ("only rows with `source_job_id IS NULL` are eligible; job-bound rows require their exact job id and were not read"), so a canonical whose only planned rows are job-bound returns checked/applied = 0 and writes nothing — never a silent broadening, and no verdict/proof can be created for those rows. Legacy writes also CAS the exact observed subject (`id` + `status='planned'` + exact observed `source_url` + `source_job_id IS NULL`), so a concurrent job bind or source re-point yields a zero-row skip instead of an old-subject overwrite. A PARTIAL-migration fallback (the additive `source_job_id` column absent while the pre-P6 columns — including `source_url` — still exist, so no job-bound row can exist) reads the legacy column set with an explicit warning; a TRUE pre-migration schema has NO fallback and is fail-closed with zero writes and an explicit error (seventh repair).
2. **M1 — ship/admin finalization now applies the same deployment-lineage gate as scheduled reconciliation whenever a job id is present.** `runBackgroundLiveVerification` finalizes a job-bound ship only when `ok === true` AND `lineageVerified === true` AND `publicationPhase === 'live_verified'`; an ok=true legacy/uncontracted verdict with a supplied job id is logged (`without positive deployment lineage`) and finalizes nothing. `POST /api/content-studio/verify-published` applies the same positive gate when `body.jobId` is present (the response gains `interlinksWithheld: 'deployment_lineage_not_proven'` in that case); without a jobId the documented legacy path runs, which is jobless-only per H1. The gate is ONE shared helper, `isDeploymentProvenLiveResult` in the new dependency-free `lib/seoFactory/deploymentProvenLive.ts` (re-exported by `interlinkReconciliation.ts` for existing importers), so the three surfaces cannot drift and there is no static import cycle.
3. **M2 — staging rebind is a revision CAS, not last-writer-wins.** Staging reads the observed revision (`source_job_id` + `staged_at`, with the partial-migration fallbacks) and every staging/rebind UPDATE carries a compare-and-set fence on that exact observed subject: exact `source_url` (or `IS NULL` for a never-staged row), exact `source_job_id` (or `IS NULL`), and exact observed `staged_at` where the column is available. A newer concurrent bind/stage therefore makes an older ship's UPDATE affect zero rows: it is counted as a concurrency skip, never as a successful rebind, and the newer job/staged_at survive untouched. Same-source/same-job no-op semantics and the existing staged_at behavior (written on first stage/rebind, withheld when the job/column is unavailable) are unchanged.
4. **Low-risk cleanups.** (a) `isSchemaUnavailable` is signature-narrowed: the message must be a schema-cache/undefined-column/undefined-relation error AND name an actual P6 column (or `seo_interlinks`); a generic "does not exist" from any other object is a real error again instead of a green `unavailable` run. *(Narrowed further by the seventh repair below: a relation miss must name the additive `seo_interlinks` relation/table and a column miss must name a known P6 additive column — a NON-P6 column of `seo_interlinks` stays a real error.)* (b) `summary.finalized` increments only when the finalizer actually checked > 0 rows; a zero-row no-op is surfaced as `checked: 0` in the detail instead of fake progress. *(Tightened by the seventh repair below: `checked > 0` alone is no longer enough — `finalized` counts only a really WRITTEN verdict/applied outcome, and the detail carries `written`.)* (c) Interlink-injection observability now distinguishes a verifier that could not run (`verifierUnavailable: true`) from a SUCCESSFUL verification that proved nothing live (all candidates withheld), in both the helper results and the pipeline/pipelineStream messages, and `withheld` counts DISTINCT normalized URLs so trailing-slash variants of one dead target cannot inflate it. (d) `POST /api/content-studio/suggest-brief` (UI/brief-only, outside the writer path) now prunes provider/profile/gig citation URLs through the same shared liveness helper and derives its author pack from the pruned citation state — author metadata survives and an unproven `marketplaceUrl` is omitted (never `''`) — so the residual is closed rather than documented.

Repair evidence: temporary no-dependency Node harness over the REAL changed modules (only Supabase/planner/link-validity/liveVerify boundaries stubbed; specifiers resolved by a temporary loader) **51 PASS / 0 FAIL**, covering: H1 jobless-only SELECT, a job-bound-only canonical returning zero checked/applied with zero writes, legacy finalization of a jobless row, and both deterministic after-select races (source re-point → skipped; job bind → skipped, job survives); M2 first-stage/rebind revision-CAS filters, same-job no-op stamp preservation, and three deterministic after-select races (observed null → newer exact-job bind survives; observed job A rebind → newer job B survives; jobless first stage → exact-job bind survives); M1 background runner (job-bound ok=true without lineage → no finalization, proven lineage → job-bound finalization, jobless legacy → jobless finalization, ok=false → none); reconciler `finalized`/`checked` zero-row truth; narrowed schema detection; shared-gate semantics; and injection `withheld`/`verifierUnavailable` normalization. Focused Jest regressions were also authored/updated for every repaired behavior (H1 scope/race suite in `tests/p6-interlink-finalize-live-proof.test.ts`; M2 races in `tests/p6-interlink-revision-staging-stamp.test.ts` plus updated CAS filter expectations in `tests/p6-interlink-staging-vs-applied.test.ts`; M1 cases in `tests/p6-interlink-background-finalize.test.ts` and `tests/p6-interlink-admin-job-binding.test.ts`; zero-row `finalized`/schema-narrowing cases in `tests/p6-interlink-durable-reconciliation.test.ts`; withheld/observability normalization in `tests/p6-interlink-draft-injection-fail-closed.test.ts` and `tests/p6-interlink-provider-link-liveness.test.ts`; `tests/helpers/p6InterlinkFakeDb.ts` gained `.is(col, null)` support) — but they were NOT executed (Jest unavailable). `node scripts/migration-ledger-policy.mjs --check` exit 0; `node scripts/migration-order.mjs --json` keeps `20260920130000` after `20260920120000_...`; `git diff --check e44fa019..HEAD` clean; exhaustive applied-writer + finalizer-caller search still shows exactly one `status: 'applied'` writer and three finalizer callers; no migration change is required by this repair (the authored migration is untouched, additive/idempotent, no DML). No dependency install, no manifest/lockfile change, no push/PR/merge/deploy, and no Jest/tsc/CI run claimed.

**Seventh final release-gate repair (post-`c7dbf22f` independent review; ONE ordinary local commit, no history rewrite, no push/PR/merge/deploy, no migration apply, no production mutation).** P6 stays **IN_PROGRESS**, P5 **PASS** untouched, P7+ **PENDING**, CREATE frozen.

1. **M1 — the non-stream post-persist JOBLESS window is closed without ever guessing a job id.** `shipContent` runs BEFORE `persistPipelineJob` creates/updates the durable `content_jobs` row, so the ship could only stage planned interlinks WITHOUT job identity — and the scheduled reconciler deliberately never auto-finalizes a jobless row (H1), leaving a successful ship unresolved. New dependency-free `lib/seoFactory/postPersistInterlinkBind.ts` re-runs the SAME planned-only `stageEngineInterlinksForVerification` immediately after persist returns the exact durable id, with the exact `plan.canonicalUrl`, the exact persisted `jobId`, `primaryKeyword` and the shipped body: a jobless planned row is REBOUND to its real ship job (`source_job_id` + a fresh `staged_at`, CASed on the observed jobless revision) and nothing else. Guards: only a real, non-dry SUCCESSFUL ship binds, and only the exact statuses where `ship.ts` itself stages (`deployed`, `merged`); a dry run, a withheld/failed ship, an unmerged `pr_created` (the ship surface staged nothing — reported as `ship-surface-staged-nothing`, nothing invented) and a ship that already carried an exact job id (`ship-already-job-bound`) bind nothing; the persisted id must pass the shared exact-UUID predicate (`normalizeSourceJobId`) and a malformed/`plan-*` id is a truthful skip — NEVER a synthetic identity. The pass never throws and never fails a successful content ship: a degraded rebind is observability only (`PipelineResult.interlinkPostPersistBind` + a structured console line). Both pipeline surfaces call it (the stream normally skips because its early row already supplied the exact id; when that early row could not be created the same window is closed the same way). `input.cluster?.existingJobId` is deliberately NOT used as ship/job identity anywhere: the pipeline persists it only as `gsc_json.cluster` METADATA — the canonical row the expansion was resolved FROM, not the `content_jobs` row this run rewrites — so the existing ownership/cluster semantics cannot prove it is this ship's subject; a focused regression pins that (the ship call and the bind call contain no cluster reference, and the sole `existingJobId: input.cluster.existingJobId` assignment sits inside the `gsc_json.cluster` snapshot, never at the persisted-payload indent).
2. **M1 telemetry — jobless staged rows are counted truthfully and cannot consume bounded scan slots.** `reconcileStagedInterlinks` gained additive `missingJobIdentityRows` + `missingJobIdentityError` from a cheap SEPARATE scalar `count: 'exact'` / `head: true` probe (`status='planned'` + `source_url IS NOT NULL` + `source_job_id IS NULL`), injectable via `deps.countJoblessStagedRows` and surfaced in both cron dockets (`interlinkMissingJobIdentityRows`, `interlinkMissingJobIdentityError`). Those rows are still never auto-finalized and their job is never guessed; a probe failure is explicit and cannot turn the pass red or fake a zero.
3. **M2 — the admin verify badge can no longer say only "Verified" when interlink finalization was withheld.** New dependency-free `lib/seoFactory/verifyStampMessage.ts` keeps the article-level success and states the withheld interlink state (`Article verified · HTTP 200 · … · interlinks pending deployment lineage`); the verify-published route shapes `stamp.message` with it whenever it returns `interlinksWithheld: 'deployment_lineage_not_proven'`, and `runVerify` in `components/design/admin-content-studio.tsx` now types `interlinksWithheld` and renders that message on its article-success (`stage: 'ok'`) path — the old bare `Verified` notice fallback is gone and nothing implies the interlinks were finalized.
4. **LOW fidelity cleanups.** (a) `summary.finalized` counts only a REAL written verdict/applied outcome (`applied + absent + targetNotLive + unverifiable + sourceNotLive`; the detail carries `written`), so a source fetch or DB-write failure that checked rows and wrote nothing can no longer count as finalized progress. (b) `isSchemaUnavailable` is narrowed further: a relation miss must name the additive `seo_interlinks` relation/table and a column miss must name a known P6 additive column — a NON-P6 column of `seo_interlinks` (e.g. `target_url`) and every other missing object stay REAL errors. (c) The disposition CLI now actually classifies through `classifyLiveStatus` (the raw `verifyUrlsLive.ok` is 2xx/3xx only and does NOT include the authority-host exemptions), and `p6ObservationFromLiveCheck` documents the raw-HTTP vs classified split truthfully. (d) Pre-migration wording now matches behavior: the staging/finalization fallbacks are described as the PARTIAL migration they are (`source_job_id`/`staged_at` absent while `source_url` exists), a TRUE pre-migration schema (no `source_url`) is explicitly fail-closed with zero writes and an observable error, and the same signature narrowing was applied to `loadEngineInterlinksForCell` (only an error that NAMES `verification_state` is the known partial state; anything else fails closed with its real error instead of being mislabelled). No jobless/job-bound fence, no proof rule, no applied writer and no applied constraint was weakened by any of it.

**Migration / DML / status.** No migration change and no DML: `20260920130000_seo_interlinks_verification_truth.sql` stays authored/unapplied, additive/idempotent/backfill-free, and the fail-closed `status='applied'` CHECK, the grants and every production row are untouched. No dependency install, no manifest/lockfile change, no push/PR/merge/deploy, no Supabase/production mutation.

Repair evidence (no Jest/`tsc`/CI claim — both remain unavailable locally and unproven everywhere): a temporary no-dependency Node harness over the REAL changed modules (only Supabase/planner/link-audit/liveVerify boundaries stubbed, specifiers resolved by a temporary loader) ran **107 PASS / 0 FAIL** in five scenarios — `bind` 16/16 (the REAL stager + the REAL bind module against a fake PostgREST client: ship-time staging is jobless, the post-persist pass rebinds the row to the exact persisted id with ONLY `source_job_id` + `staged_at` in the patch and a CAS on the observed jobless revision, an already-bound ship writes nothing, and dry-run/no-ship/unmerged-PR/malformed-id write nothing), `recon` 18/18 (finalized counts only written buckets; a checked-but-unwritten failure is `finalized 0` with `written 0`; the scalar jobless probe is a `count:exact+head` read with the exact three filters; the jobless rows never consume scan slots; a failing probe is explicit and non-red; the narrowed schema signatures), `route` 11/11 (the REAL verify-published route: a withheld lineage keeps `stamp.status: 'verified'` while the message reads `Article verified … interlinks pending deployment lineage` and finalization is not invoked; a proven `live_verified` verdict finalizes job-bound with no interlink suffix; ok=false and no-jobId legacy behavior unchanged), `static` 54/54 (helper semantics + every authored source assertion: pipeline/stream call order and exact arguments, no cluster identity, ship.ts contract, planned-only bind module, cluster metadata fidelity, UI/route typing, CLI classification, reconciliation write-verb/applied-literal absence, cron telemetry, fake-DB count support, partial-migration wording), `engine` 8/8 (the REAL `loadEngineInterlinksForCell`: a `verification_state`-naming error still retries the legacy select, a generic missing object and a permission failure both fail closed with their real error and no retry). `node scripts/migration-ledger-policy.mjs --check` exit 0; `node scripts/migration-order.mjs --json` still lists 75 entries with `20260920130000_seo_interlinks_verification_truth.sql` directly after `20260920120000_seo_cannibal_decisions_append_only_search_path.sql`; type-stripped `node --check` parse of every changed `.ts`/`.mts` (the `.tsx` admin component cannot be parsed by `node --check` because it contains JSX (pre-existing); its edited, JSX-free `runVerify` block was extracted into a wrapper and parse-checked with `node --check`); exhaustive grep still shows exactly one `status: 'applied'` writer (`lib/seoFactory/interlinkVerification.ts`) and exactly three finalizer callers (`liveVerify.ts`, `interlinkReconciliation.ts`, the verify-published route); `git diff --check e44fa019..HEAD` clean. The temporary harness (`.p6-harness/`, untracked) was removed before the commit. **PR CI still has NOT been run for P6** (branch not pushed, no PR).

**Branch shape (truthful).** This branch is now **nine commits above base** `e44fa01944423babe906c45f36c06bf4b6a8b4d4`: `e9bfe607` (foundation) → `c7f2b43d` (first supervisor-review repair) → `eb2b1133` (test-lifecycle hardening) → `f3e83e9f` (docs correction) → `403ba549` (second supervisor repair) → `f9af3ad8` (third supervisor follow-up repair) → `881377ca` (fourth pre-PR repair) → the fifth release-blocker repair commit → this ordinary sixth pre-push correctness-repair commit. The intended in-place amend of `e9bfe607` was denied by the runtime's Git broker (no destructive-git authority; no bypass attempted), so **nothing here is amended in place and the branch is not a single commit today**. Collapsing later needs no further local history rewrite: `git rebase -i --autosquash e44fa019` under destructive-git authority, or a normal **squash-merge** landing one commit on `main`.

**CI status (truthful).** Jest and `tsc` are unproven **everywhere**: they could not run locally (`node_modules` resolves to an empty directory; no install attempted, no manifest change) and **PR CI has not yet been run for P6** — the branch has not been pushed and no PR exists. PR CI remains required and unexecuted; nothing at this checkpoint is claimed proven by CI.

## Accepted residuals (recorded, NOT claimed PASS)

1. **Pre-migration fail-closed state (observable now).** Until `20260920130000_seo_interlinks_verification_truth.sql` is applied, `loadEngineInterlinksForCell()` retries the legacy select without `verification_state` (verdicts treated as unknown) with an explicit warning, and any other DB failure fails closed with a warning — so a missing column/permission problem can no longer look like a genuinely empty cell. The reconciliation seam still reports `unavailable` (reason = missing column) instead of verifying anything or turning the daily run red. Nothing is applied or claimed in that state.
2. **Degraded anon service-role mode is now a hard prerequisite.** The migration's least-privilege grants assume server writes really run as `service_role`. If the deployed key resolves to the anon key (`isServiceRoleAchieved() === false`; legacy-JWT service key missing), previously the open `FOR ALL` policy let it write; after the migration those writes fail — and the new staging/verdict paths surface the failure truthfully (`failed`/`dbErrors` + `error`) rather than looking like a no-op. Applying the migration therefore requires verifying healthy service-role auth first.
3. **Legacy/API-only surfaces not migrated.** `app/api/seo-engine/interlink/route.ts` (admin manual plan persistence) now reports the truthful `filtered` count but still generates+persists plan metadata directly; `lib/seoEngine/backlinkEngine.ts`, `app/api/content-studio/interlinks/route.ts`, `app/api/content-studio/system-health/route.ts` and `app/api/seo-engine/status/route.ts` remain read-only consumers of raw `seo_interlinks` counts. None of these is a second `applied` writer (exhaustively scanned), and none is claimed verified.
4. The 1,899-row production backlog remains unclassified/unreconciled, source identity remains unresolved for 204 of 206 pre-existing sources, and the ≥80% gate stays unevaluable — all unchanged by this repair.
5. **Revision stamp requires the migration.** `staged_at` (and every other P6 verification column) is still unapplied. Rows staged while the column is unavailable stay job-bound without a revision stamp; the reconciler then uses the documented `updated_at` fallback for revision membership — truthful but weaker than a real stamp (the trigger-later cooldown bug persists for those legacy rows until the migration is applied and they are re-staged).

## Explicitly not done (frozen)

- No migration applied, no Supabase/production mutation, no `seo_interlinks` row changed.
- No production reconciliation/rejection/retarget, no backlog classification run against production.
- No push, PR, merge or deploy.
- Broad net-new CREATE remains frozen until P13.

## Required P6 work still open

1. Apply the migration through the official workflow and prove the ledger entry (supervisor).
2. Authorized read-only disposition run against production; then the bounded reconciliation/rejection/retarget decision for the 1,643 404 and 183 legacy auth-wall rows (separate authorization; **not** part of this foundation commit).
3. Move rows from `planned` to proven `applied`: after a ship the immediate background path finalizes job-bound when the deployment already satisfies `verifyLiveUrl` (`verifyLiveInBackground` → `runBackgroundLiveVerification`), the **scheduled** pass re-verifies exact `(source_url, source_job_id)` subjects automatically once the deployment is observable (`reconcileStagedInterlinks` on the daily cron; no operator click required) and finalizes ONLY with positive official deployment-lineage proof (an `ok=true` legacy/uncontracted health verdict stays `notDeploymentProven`), and the admin `verify-published` path remains available (job-bound when it receives a jobId). Rows with no job identity (historical/backlog, or staged by a legacy caller) remain unresolved/manual, are excluded from the scheduled scan, and still need an authorized verification run.
4. Re-evaluate the ≥80% gate only after the approved-useful denominator is defined from real live-target evidence.

## 2026-09-21 — Post-merge reconciliation: Batch A tool authored, NOT executed

**Status: IN_PROGRESS — never PASS.** P5 PASS untouched, P7+ PENDING, broad net-new CREATE frozen until P13. Branch `seo/p6-backlog-reconciliation-20260921` at base `ab6305a7c5c362f01b46947e53be0e462772c88e` (the merged `main` tip), **two ordinary local commits** (no history rewrite, no amend): commit 1 = the Batch A tool + this documentation; commit 2 = registering the new authorized writer in the exhaustive least-privilege audit (`tests/p6-interlink-least-privilege.test.ts`), which the first commit had not anticipated and which cannot be folded back because amend/history-rewrite authority was not granted. The branch is squash-mergeable to one commit on `main` without further local history rewriting.

**Post-merge production truth.**

- PR #256 merged to `main` as `ab6305a7c5c362f01b46947e53be0e462772c88e`; required checks passed before merge.
- `Apply SEO Factory Migrations` run `35556091287` applied `20260920130000_seo_interlinks_verification_truth.sql` successfully. The additive verification-truth columns (`source_url`, `source_job_id`, `verification_state`, `verified_at`, `verification_evidence`, `staged_at`, `verification_attempted_at`) now exist in production; the fail-closed `status='applied'` constraint, the closed verification vocabulary, the partial indexes and the least-privilege grants are live.
- `Deploy YouSafe Portal` run `35556091282` is green.
- Read-only audit (post-merge, no mutation): **1,899** `seo_interlinks` rows all `planned` — **0 applied, 0 rejected, 0 gated**; **236** distinct targets / **206** source slugs; **1,643** rows across **186** targets returned HTTP 404; **183** rows across **47** `portal.yousafeconsultancy.com` targets are the legacy auth wall; **73** rows across **3** market-category targets are live 200 with source unverified; **0** rows carry `source_url`, `source_job_id`, `staged_at`, a verification verdict/proof or `applied_at`.
- "Required P6 work still open" item 1 (apply the migration through the official workflow and prove the ledger entry) is therefore **complete**. The ≥80% gate remains unevaluable: 0 verified applied.

**Batch A scope (authorized mutation path, not yet run).** Historical **jobless** planned rows whose exact current target freshly proves **HTTP 404 or 410 only**. Not Batch A, left read-only/report-only: legacy Portal auth-wall rows (even if they answer 404/410), live targets, unknown/network/5xx/401/403/405/429 targets, and any row that has acquired a source identity, job identity, staging stamp, attempt marker, verdict/proof or `applied_at`.

**Deliverables added.**

- `scripts/p6BatchAStaleRejection.ts` — pure logic: subject fence (`planned` + nonblank `target_url` + strict NULL on `source_url`/`source_job_id`/`verification_state`/`verified_at`/`verification_evidence`/`verification_attempted_at`/`staged_at`/`applied_at`), 404/410-only dead classification (authority-classified 2xx/3xx and the 401/403/405/429 exemption = live; everything else unknown), deterministic `(created_at, id)` ordering with id dedupe, bounded selection, exact CAS-fence construction, the rejected-disposition patch, and strict argv parsing.
- `scripts/p6BatchAStaleRejectionRunner.ts` — dependency-injected orchestration (read/observe/write/count/clock), fail-closed on read error/truncation and on global verification failure, zero write calls in dry run, abort-on-first-write-error, per-row CAS skip accounting.
- `scripts/p6-batch-a-stale-rejection.mts` — executable wrapper: SELECT-only candidate read with the jobless/no-proof SQL fence and truncation probe, fresh per-run probes through the repository authority (`verifyUrlsLive` HEAD→GET fallback + `classifyLiveStatus`), and the exact-row CAS `UPDATE` only in apply mode.
- `tests/p6-batch-a-stale-rejection.test.ts` — pins dry-run zero-write construction, 404/410-only dead classification, 200/401/403/405/429/0/5xx/other untouched, the exact CAS fence, the bounded hard maximum, deterministic ordering, idempotency, fail-closed reads/verification, and no proof-field fabrication.
- `tests/p6-interlink-least-privilege.test.ts` (updated) — the exhaustive `seo_interlinks` writer audit now registers the operator-run Batch A CLI as a third authorized server-side writer and pins that its only write is the fenced `status='rejected'` stale disposition (`.select('id')` read-back, no `status: 'applied'`, no upsert/insert/delete/rpc).

**Write contract (each write is exact-row CAS).** `status='rejected'` plus the existing auditable `gate_reason` (`stale_target_http_404` | `stale_target_http_410`), `gate_actor` (`p6-batch-a-stale-rejection`) and `gate_updated_at`; fenced on `id` + `status='planned'` + the exact observed `target_url` + `IS NULL` for all eight identity/proof/staging columns. A concurrent change yields a zero-row update, counted as `casSkips` — never success. No proof/identity/staging column is ever written or fabricated, no schema change, no insert/delete/upsert/rpc.

**Execution controls.** Dry run is the default and contains zero update/upsert/delete/rpc calls; apply requires `--apply --confirm REJECT-BATCH-A-STALE-404-410` (no environment-only write mode); `--limit` defaults to 50 and is hard-capped at 200 per invocation; ordering is deterministic so reruns continue with the next dead rows; rejected rows are excluded from the candidate predicate, so a rerun never re-touches them.

**Verification for this checkpoint (no Jest/`tsc` — `node_modules` is an empty linked directory; no install attempted).**

- The committed test file was executed through a type-stripping Node harness with a minimal Jest shim over the REAL modules: **34 PASS / 0 FAIL**.
- The updated exhaustive writer audit (`tests/p6-interlink-least-privilege.test.ts`) was executed through the same shim against the real repository tree: **10 PASS / 0 FAIL**.
- A separate no-dependency harness over the same real modules (dry-run zero writes, 404/410-only, CAS fence/patch key set, bounds, ordering, idempotency, fail-closed) ran **34 PASS / 0 FAIL**.
- `node --check` parses every new `.ts`/`.mts` file; exhaustive static assertions prove the pure module and runner contain no write verb and no Supabase import, the runner returns before any write call in dry run, and the CLI wires `verifyUrlsLive` + `classifyLiveStatus` with no upsert/delete/rpc and no raw `method: 'HEAD'`.
- `node scripts/migration-ledger-policy.mjs --check` exit 0; `node scripts/migration-order.mjs --json` keeps `20260920130000_seo_interlinks_verification_truth.sql` last after `20260920120000_seo_cannibal_decisions_append_only_search_path.sql`; **no migration was added or changed**.
- The tool was never invoked — no command was run with apply mode, no network/DB call was made, and no `seo_interlinks` row was written.

**Residuals / pre-execution prerequisites for the operator.** (1) The write path requires effective service-role auth — now a HARD, fail-closed prerequisite enforced before any client is created (see the 2026-09-21 pre-push safety-hardening addendum); the migration's least-privilege grants additionally mean a degraded anon-key client would fail closed with DB errors, not silent success. (2) The candidate read requires the applied P6 columns; a pre-migration projection errors and fails closed. (3) `gate_reason` vocabulary is new to operators (`stale_target_http_404` / `stale_target_http_410`) and is additive audit metadata, not an applied proof. (4) Batch A clears only the jobless/no-proof dead subset; the wider backlog (live-source-unverified, legacy auth wall, staged-but-unfinalizable) still needs the separately authorized disposition/reconciliation work. (5) The ≥80% gate stays unevaluable until verified-applied volume exists.

## 2026-09-21 — Batch A pre-push safety hardening (third ordinary local commit; Batch A still NOT executed)

**Status: IN_PROGRESS — never PASS.** P5 PASS untouched, P7+ PENDING, broad net-new CREATE frozen until P13. Same branch `seo/p6-backlog-reconciliation-20260921` (base `ab6305a7c5c362f01b46947e53be0e462772c88e`), now **three ordinary local commits** (no history rewrite, no amend, no rebase; commits 1–2 above are untouched): commit 3 records this supervision-requested pre-push safety hardening. No push, PR, merge, deploy, migration apply, dependency install, Supabase write, production mutation or apply-mode execution in this checkpoint. The branch is still squash-mergeable to one commit on `main`.

**Blocker 1 — runner-side confirmation gate (defense in depth).** `runP6BatchARejection` no longer trusts argv parsing: it now refuses any truthy `config.apply` whose `config.confirm` is not EXACTLY `P6_BATCH_A_APPLY_CONFIRM_TOKEN` — placed before the limit validation and therefore before any status count, candidate read, live probe or write. A direct programmatic call with `apply: true` plus a missing, wrong, blank, whitespace-padded or case-shifted token makes **ZERO** IO calls and returns a fatal fail-closed summary (`failedClosed: true`, `attemptedWrites: 0`, `selectedForWrite: 0`, `writeResults: []`) whose fatal error names the required token. Tests cover both wrong-token variants and the exact-token control, and prove dry run (no token needed) still never writes.

**Blocker 2 — genuine service-role authority is a hard prerequisite for APPLY.** Key resolution no longer runs at module load: the CLI parses argv first and only then resolves the key. New dependency-light `scripts/p6BatchAApplyAuthority.ts` owns the apply-authority decision — require `supabaseAuthMode() === 'service-role'` AND `resolveSupabaseKey({ allowAnonFallback: false })` returning a usable legacy `eyJ…` JWT. Dry run keeps the existing read-capable anon fallback; apply mode NEVER inherits it — a degraded/anon/missing authority exits nonzero before `createClient`, so no write-capable client is constructed and no DB/network call is reachable. `decideP6BatchAApplyAuthority` is a pure decision over injected `authMode`/`key`, unit-tested without secrets, including the exact production trap (new-format `sb_secret_…` service key + usable legacy anon key → refused).

**Unchanged contracts (re-pinned by the same committed test file).** Exact-row CAS; the historical jobless/no-proof NULL subject fence; legacy Portal auth-wall exclusion; 404/410-only dead classification (0/5xx/401/403/405/429/other stay unknown); hard max 200 rows; deterministic `(created_at, id)` ordering; abort on first write error; no proof/identity/staging field written or fabricated; dry-run default with zero write calls.

**Verification for this hardening checkpoint (no Jest/`tsc` — `node_modules` is an empty linked directory; no install attempted).**

- The committed `tests/p6-batch-a-stale-rejection.test.ts` ran through a type-stripping Node harness over the REAL modules: **44 PASS / 0 FAIL** (was 34; +10 authorization/authority cases: runner gate with zero IO, wrong/near-miss tokens, exact-token control, untouched dry run, pure + env-backed authority decisions).
- The updated least-privilege writer audit (`tests/p6-interlink-least-privilege.test.ts`) ran through the same harness against the real tree: **10 PASS / 0 FAIL** (the new helper adds no `seo_interlinks` writer; the CLI's single fenced update is unchanged).
- End-to-end CLI refusal matrix with a harness-stubbed `@supabase/supabase-js` and stubbed link authority (**no network, no DB**): `--help` → exit 0, zero clients; `--apply` (no confirm) → exit 2, zero clients; `--apply --confirm WRONG` → exit 2, zero clients; authorized apply with missing keys → exit 1 "requires genuine service-role authority", zero clients; authorized apply with anon-only and with the `sb_secret_`+legacy-anon trap → exit 1, zero clients; authorized apply with a fake legacy service-role JWT → client stub constructed, then the stubbed DB throws → fail-closed JSON summary (`mode: "apply"`, `attemptedWrites: 0`, `writeResults: []`, `failedClosed: true`) with no write verb reached; dry run with the anon fallback still constructs the read client and fails closed on the stub error.
- `node --check` parses every changed/added `.ts`/`.mts`; `node scripts/migration-ledger-policy.mjs --check` exit 0; `node scripts/migration-order.mjs --json` reports 75 entries with `20260920130000_seo_interlinks_verification_truth.sql` still last after `20260920120000_seo_cannibal_decisions_append_only_search_path.sql`; **no migration was added or changed**.
- `git diff --check ab6305a7..HEAD` clean; worktree clean after commit 3.
- The tool was never executed: no real Supabase client was constructed (harness stub only), no network call was made, no command ran apply mode against a live key, and no `seo_interlinks` row was read or written. **Batch A remains authored-but-NOT-executed; the ≥80% P6 gate remains unevaluable until verified-applied volume exists.**

## 2026-09-21 — Batch A proof hardening: exact-target binding + mandatory GET re-confirmation (Batch A still NOT executed)

**Status: IN_PROGRESS — never PASS.** P5 PASS untouched, P7+ PENDING, CREATE frozen. Short-lived branch `seo/p6-batch-a-proof-fix-20260921` at base `96abd1b2ee6f41ba3b04062e937e149db47814f9` (the Batch A commit already on `main`), **one ordinary local commit** (no amend/rebase/history rewrite). No push, PR, merge, deploy, migration apply, dependency install, Supabase read/write, production mutation or apply-mode execution. This section supersedes the earlier proof wording (a raw HEAD 404/410 through `verifyUrlsLive` + `classifyLiveStatus` was described as sufficient proof).

**Proof contract now enforced (all four together).**

1. **EXACT BINDING.** A proof is bound to the EXACT trimmed stored absolute http(s) `target_url` that was probed — never a normalized, case-folded, host-normalized or trailing-slash-folded key. `https://host/a/` and `https://host/a` are two Batch A targets and each needs its own observation; an observation for one can never reject a row spelled the other way.
2. **NONCANONICAL TARGETS UNTOUCHED.** Relative paths, bare hosts, `mailto:`/`ftp:`/`javascript:`/other schemes and any non-absolute value are `noncanonical_target`: never probed, never written, counted truthfully. Only parseable absolute `http:`/`https:` URLs with a non-empty host are eligible.
3. **HEAD 404/410 IS NEVER SUFFICIENT.** The primary probe keeps the repository authority (`verifyUrlsLive` HEAD with the existing 403/405/501 GET retry, verdict via `classifyLiveStatus`), but every target it classifies raw 404/410 MUST be explicitly re-confirmed by a GET-only check of the same exact URL (`verifyUrlsLiveGet`, new in `lib/seoFactory/linkAuditCore.ts`, re-exported by the `linkAudit` facade) that neither reads nor populates the shared HEAD cache and refuses relative/non-http(s) inputs instead of synthesizing a target. Only a raw GET 404/410 is selectable; a HEAD-dead target with a GET 2xx/3xx/0/other verdict is `head_dead_unconfirmed` and untouched; a missing/throwing/empty re-confirmation authority fails the run closed with zero writes. The GET verdict is the recorded proof and `gate_reason`.
4. **RAW CAS FENCE.** The write still fences the exact RAW STORED `target_url` (the trimmed value is only the proof key), plus `id`, `status='planned'` and `IS NULL` on all eight historical jobless/no-proof columns; a zero-row UPDATE is a concurrency SKIP.

**Preserved unchanged:** genuine service-role apply authority (`supabaseAuthMode() === 'service-role'` AND a legacy `eyJ…` key via `resolveSupabaseKey({ allowAnonFallback: false })`) enforced after argv parsing and before `createClient`; the exact `--apply --confirm REJECT-BATCH-A-STALE-404-410` pair as the only write switch (no environment-only switch); hard max 200 rows per invocation; deterministic `(created_at, id)` pagination with a proven truncation probe; the strict historical jobless/no-proof subject fence; legacy Portal auth-wall exclusion; first-write abort; dry-run default with zero writes; no proof/identity/staging field fabricated (`status='rejected'` + `gate_reason`/`gate_actor`/`gate_updated_at` only).

**New executable boundary.** `scripts/p6BatchACliBoundary.ts` owns argv → apply-authority → client → runner with every boundary injected (no environment read, no Supabase import, no network, no write verb), so the exit codes and the zero-client fail-closed paths are unit-executable with fakes; `scripts/p6-batch-a-stale-rejection.mts` is now only the real wiring.

**Local substitute evidence (Jest/`tsc` unavailable: `node_modules` is an empty linked directory; `npx --no-install jest` cannot resolve, no install attempted, no network).** Committed `tests/p6-batch-a-stale-rejection.test.ts` executed through a type-stripping Node harness over the REAL modules: **69 PASS / 0 FAIL** (exact-binding, noncanonical, HEAD-vs-GET matrix, GET re-confirmation authority with a deterministic fetch stub, CLI boundary exit codes 0/1/2, key-selection and pagination pins). `tests/p6-interlink-least-privilege.test.ts` through the same harness: **10 PASS / 0 FAIL** (the new boundary adds no `seo_interlinks` writer). End-to-end execution of the REAL `.mts` wrapper with a stubbed `@supabase/supabase-js` PostgREST stub + deterministic fetch stub (no socket, no network, no DB): **51 PASS / 0 FAIL** — dry run selects exactly the HEAD-404+GET-404 row, leaves the HEAD-404+GET-200 and noncanonical rows untouched, makes ZERO update calls, reads with the 8 NULL fences/`(created_at, id)` order/bounded range, re-confirms both head-dead targets by GET; apply with a legacy service-role key makes exactly ONE fenced update whose `eq` fence carries the RAW padded stored `target_url` while HEAD+GET hit the trimmed exact URL, patch keys are exactly the four disposition/audit fields, and a degraded-anon apply exits nonzero with zero clients/fetches. `node --check` parses all six changed/added `.ts`/`.mts` files; `git diff --check` clean. Residual: no real estate probe, no real Supabase client, no apply execution, Jest/`tsc`/PR CI still unproven. **Batch A remains authored-but-NOT-executed; the ≥80% P6 gate remains unevaluable.**
