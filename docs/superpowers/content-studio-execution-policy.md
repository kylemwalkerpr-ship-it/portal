# Current executor and delivery policy — 2026.09.23.7

Effective user direction, 23 September 2026. This policy supersedes prior executor routing/defaults; it does not waive repository, security, SEO, review or release gates.

- GPT Sol owns architecture, scope, steering, independent verification and final merge authority.
- Refactor execution uses native **Codex Luna subagents at high reasoning effort**. Resolve the exact available Luna model identifier (currently `gpt-6-luna`); record it with `reasoningEffort=high` in every packet. Missing capability means BLOCKED_CAPABILITY, not an automatic fallback.
- DeepSeek, Grok and Freebuff are not current refactor executors or fallback lanes. Existing external jobs, leases and user-owned work must remain untouched. Historical harness instructions apply only to separately authorized maintenance of that harness.
- Start with at most three concurrent Luna workers, bounded by actual capacity. One writer per worktree/conflict domain; explicit dependencies for shared schema/auth/routing. Separate read-only reviewer identity; Sol verifies critical raw evidence and the final candidate.
- Every packet records task/revision, executor policy, actual repo/worktree, base SHA, bounded paths, exclusions, dependencies, acceptance, source versions, allowed capabilities, phase evidence, CPU evidence where relevant, independent reviewer and recovery state. A timeout requires reconciliation before retry; no duplicate or uncertain writes.
- P0–P11 **code is complete/PASS** as user-confirmed and supported by dated repository records. P0–P8 recorded program gates PASS; P9–P11 remain IN_PROGRESS; P12–P13 PENDING. Separate code, CI, deployment, live acceptance and outcomes. Never redo passing phase implementation merely because outcome proof is pending.
- P9 needs verified live backlink outcome; P10 needs the required real production paid-order event; P11 needs reconciliation of older outstanding delivery facts and current GEO observation proof. Do not invent a deployment, conversion, citation or gate closure.
- Current architecture is CS-2026.09.23.7 on Portal `content-studio-revamp`; the normative spec, blueprint, final audit and manifest use their existing 2026-09-22 paths. New delivery schema/state use 2026-09-23 paths. Re-pin current main and check the canonical parity matrix/ledger before admitting work.
- Initial kickoff is read-only source/state/readiness reconciliation. Operational A0 or implementation waits for applicable program entry authorization, or a separately explicit narrowly scoped exception. No inference of such exception from this document. Broad CREATE, qualified YMYL review, slug ownership, release permits and publication gates remain binding.
- Approved code follows scoped PR → exact-head required checks → Sol merge approval → official deployment → independent live proof. Never local direct deployment, forced Git changes, unrelated cancellation or ungranted production writes.
- Modal is the primary heavy-compute runtime; Postgres is the sole durable run/gate authority. Luna is a development executor, not the production content-generation backend.
- HTTP Worker CPU acceptance remains total-invocation <10ms including auth/routing/serialization (p95 ≤2.5ms; p99 ≤5ms), measured across required cohorts. It is UNVERIFIED until evidence exists; documentation cannot guarantee performance.
