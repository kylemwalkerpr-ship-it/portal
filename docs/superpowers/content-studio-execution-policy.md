# Current executor and delivery policy — 2026.09.24.1

Effective user direction, 24 September 2026, under Hjarni Supervisor Arc 33465 / playbook 34597 contract **2026.09.23.7-rdc1**, with the unmerged v9 GPT-6 Luna executor update carried forward as current packet policy. This policy supersedes prior executor routing/defaults; it does not waive repository, security, SEO, review or release gates.

- GPT Sol owns architecture, scope, steering, independent verification and final merge authority.
- Refactor execution uses the **Luna family** through the standard `RDC_CODEX_CLI` local Codex CLI route. At admission, every packet re-resolves the exact current model ID and advertised effort catalog, selects a supported effort for the task, records the rationale, and verifies access to the selected effort. The catalog is not proof of account access. Historical A0.1 `gpt-5.6-luna/high` remains dated evidence in the delivery snapshot. Current executor selection is `gpt-6-luna` with task-selected `low`, `medium`, `high`, `xhigh` or `max` only after a fresh packet-specific catalog and selected-effort capability check; the 2026-09-23 observations do not substitute for that probe. A missing model or selected-effort capability is `BLOCKED_CAPABILITY`; there is no automatic provider-family fallback.
- The executor policy is `studio.executor-policy/2`. It records supervisor, Luna family, exact resolved model, re-resolution mode/evidence, advertised versus environment-verified efforts, selected effort/rationale/access, `RDC_CODEX_CLI` transport, orchestration mode, native nested capability, and whether the executor is a native child. A separate CLI session is a separate executor, never a native nested child.
- DeepSeek, Grok and Freebuff are not current refactor executors or fallback lanes. Existing external jobs, leases and user-owned work must remain untouched. Historical harness instructions apply only to separately authorized maintenance of that harness.
- Start with at most three concurrent workers, bounded by actual capacity. One writer per worktree/conflict domain; explicit dependencies for shared schema/auth/routing. The reviewer must be distinct from the writer; Sol verifies critical raw evidence and the final candidate.
- Every packet records task/revision, executor policy, actual repo/worktree, base SHA, bounded paths, exclusions, dependencies, acceptance, source versions, allowed capabilities, phase evidence, CPU evidence where relevant, independent reviewer and recovery state. A timeout requires reconciliation before retry; no duplicate or uncertain writes.
- P0–P11 **code is complete/PASS** as user-confirmed and supported by dated repository records. P0–P8 recorded program gates PASS; P9–P11 remain IN_PROGRESS; P12–P13 PENDING. Separate code, CI, deployment, live acceptance and outcomes. Never redo passing phase implementation merely because outcome proof is pending.
- P9 needs verified live backlink outcome; P10 needs the required real production paid-order event; P11 needs reconciliation of older outstanding delivery facts and current GEO observation proof. Do not invent a deployment, conversion, citation or gate closure.
- Historical v7 source is Portal `content-studio-revamp@2af720e8290c67806f335da746f91af35dd2c5f6`. CS-2026.09.23.8 is this main-based exact-commit candidate; the normative spec, blueprint, final audit and manifest use their existing 2026-09-22 paths, and the delivery schema/state use 2026-09-23 paths. Re-pin the authoritative source by exact commit and check the canonical parity matrix/ledger before admitting work.
- Before every packet, fetch/reconcile `origin/main` and local `main`; stale or diverged state blocks admission. Codex Luna runs from the local repository/worktree via RDC/local CLI. Connector/tool availability (GitHub/Supabase/Cloudflare) is capability, not authorization; packet scopes and gates still control use.
- Dated integration capability observations are readiness evidence only: the 2026-09-23 probe recorded authenticated project/account names and statuses for GitHub, Supabase and Cloudflare under read-only scope. These observations are not authorization, entitlement, production telemetry, deployment proof, schema/RLS proof or permission to probe further; the delivery state does not treat them as live inventory.
- **2026-09-24 staged entry:** Kyle authorizes Sol to admit bounded A0.2–A0.7 discovery/read-only baselines and dependency-ordered A1–A7 construction on isolated branches without closing P9–P13. First reconcile current main, PRs, historical A0.1 integrity and current Worker incident evidence. Fresh packet scope controls each probe and code edit. Open P9–P11 outcomes remain open; P12 gates affected activation/cutover and measured Worker CPU; P13 gates eligible cluster-specific net-new CREATE. No production DB/provider/account mutation, spend, merge, deploy, external write, CREATE/publication or phase promotion follows from this entry authorization alone. Existing owner/slug/canonical, YMYL, release and security permits remain binding.
- Approved code follows scoped PR → exact-head required checks → Sol merge approval → official deployment → independent live proof. Never local direct deployment, forced Git changes, unrelated cancellation or ungranted production writes.
- Modal is the primary heavy-compute runtime; Postgres is the sole durable run/gate authority. Luna is a development executor, not the production content-generation backend.
- HTTP Worker CPU acceptance remains total-invocation <10ms including auth/routing/serialization (p95 ≤2.5ms; p99 ≤5ms), measured across required cohorts. It is UNVERIFIED until evidence exists; documentation cannot guarantee performance or claim elimination of the historical 1102 class.
