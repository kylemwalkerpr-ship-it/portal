# Current executor and delivery policy — 2026.09.23.9

Effective user direction, 23 September 2026, under Hjarni Supervisor Arc 33465 / playbook 34597 contract **2026.09.23.7-rdc1**. This policy supersedes prior executor routing/defaults; it does not waive repository, security, SEO, review or release gates.

- **Roles:** GPT Sol is supervisor and owns scope, approvals, architecture, independent review, integration and merge authority. The executor is local Codex CLI 0.156.1 through Remote Desktop Commander (RDC), model ID `gpt-6-luna`.
- **Dated capability evidence (2026-09-23):** five strict read-only one-shot CLI probes returned OK and exit 0 for `low`, `medium`, `high`, `xhigh`, and `max`. One model-catalog refresh timed out; explicit model invocation nevertheless completed successfully. This is tested evidence, not a future admission guarantee.
- **Admission policy:** at every task admission, check the current model catalog and explicitly probe the selected effort. Select the lowest currently supported effort sufficient for that task's risk and complexity, from `low` through the current maximum (currently `max`). Record the selected effort, rationale and verified access on that task packet. Do not globally pin `high` or `max`. Never silently substitute a model, model family or effort. If GPT-6 Luna or the selected effort is unavailable, stop with `BLOCKED_CAPABILITY`.
- **Schema:** the executor-policy contract is `studio.executor-policy/3`. It separates dated model/effort capability evidence from per-task effort selection. `automaticFallback` remains `false`. Native Codex subagents may be used only after native availability and exact model/effort support are verified. Otherwise use separate RDC-launched Codex CLI sessions and record `nativeChild=false`; a CLI session is not a native child.
- DeepSeek, Grok and Freebuff are not current refactor executors or fallback lanes. Existing external jobs, leases and user-owned work remain untouched. Historical harness instructions apply only to separately authorized maintenance of that harness.
- Sol owns safe Git fetch, fast-forward and parity checks, and confirms local/remote parity before task work. Preserve dirty, diverged and other-owned worktrees; never force reset. Keep code changes in scoped worktrees/branches, then use a reviewed PR and required checks. Provide authorized GitHub/Supabase/Cloudflare integrations to the executor and verify calls work inside it; capability is not operational authorization.
- Every packet records task/revision, executor policy, actual repo/worktree, base SHA, bounded paths, exclusions, dependencies, acceptance, source versions, allowed capabilities, phase evidence, CPU evidence where relevant, independent reviewer and recovery state. A timeout requires reconciliation before retry; no duplicate or uncertain writes. Use one writer per conflict domain and a distinct reviewer. Native subagents are only used when their availability and exact model/effort support are verified.
- A0.1 PR **#283** merged at `22c685e38d46367eec18332f264b7f1480d8305e`; GitHub Actions workflow **35822388398** succeeded. Worker version `b5e39685-baa2-4789-acfa-10e7f990d2ad`, build `LU03P9-X1WyePB7inlqsO`; production health passed and exact-main smoke passed **4/4**. A0.1 authorized no broader implementation.
- P9–P11 remain **IN_PROGRESS**; P12–P13 remain **PENDING**; Worker CPU remains **UNVERIFIED**. No A0.2/A0.3 admission, refactor, database change, CREATE, phase promotion or publication is authorized. P0–P11 code PASS does not imply program closure. P9 requires verified live backlink outcome; P10 the real production paid-order event; P11 reconciliation of older outstanding delivery facts and current GEO observation proof. Do not invent a deployment, conversion, citation or gate closure.
- Every main push deploys production; because a documentation merge also triggers the Worker deployment, disclose release consequences and follow the authorized merge/deploy path. Approved code follows scoped PR → exact-head required checks → Sol merge approval → official deployment → independent live proof. Never local direct deployment, forced Git changes, unrelated cancellation or ungranted production writes.
- Modal is the primary heavy-compute runtime; Postgres is the sole durable run/gate authority. Luna is a development executor, not the production content-generation backend.
- Preserve all SEO constraints, including exact slug/canonical ownership, sanitization, cannibalization, qualified YMYL review, release permits and publication gates. No bulk CREATE or publication bypass.
- HTTP Worker CPU acceptance remains total invocation <10ms including auth/routing/serialization (p95 ≤2.5ms; p99 ≤5ms), measured across required cohorts. It remains UNVERIFIED until evidence exists; documentation cannot guarantee performance or claim elimination of the historical 1102 class.
