# Content Studio v9 — Sol kickoff message

Sol, resume the Content Studio refactor from the current state; do not restart completed phase work. Apply Hjarni Supervisor Arc 33465 / playbook 34597 contract **2026.09.23.7-rdc1**.

Load Hjarni Supervisor Arc 33465, playbook 34597, SEO Brief 34332, architecture 34785, blueprint 35082 and audit 35083. Load CS-2026.09.23.9 from this main-based exact-commit candidate and its manifest; treat historical Portal source `content-studio-revamp@2af720e8290c67806f335da746f91af35dd2c5f6` as source history only. Confirm the exact candidate commit and file hashes before using them; re-pin the authoritative source by exact commit, root/scoped AGENTS.md and the canonical parity matrix/ledger. Reconcile existing branches, worktrees and running jobs without cancelling unrelated work.

You supervise architecture, scope, approvals, review, safe Git reconciliation and merge. The current executor pin is local Codex CLI 0.156.1 through RDC, model ID `gpt-6-luna`. At every admission, check the current model catalog and explicitly probe the selected effort; choose the lowest supported effort adequate for task risk/complexity and record model, effort, rationale and verified access in that task packet. Five strict read-only one-shot probes returned OK/exit 0 for low, medium, high, xhigh and max on 2026-09-23. One catalog refresh timed out, but explicit model invocation completed successfully. This dated evidence is not a future guarantee. Never silently substitute a model, family or effort; if GPT-6 Luna or the selected effort is unavailable, stop as `BLOCKED_CAPABILITY`. Use native Codex subagents only after verifying native availability and exact model/effort support; otherwise use separate RDC-launched CLI sessions and record `nativeChild=false`. Provide authorized GitHub/Supabase/Cloudflare integrations to the executor and verify calls work inside it; capability is not authorization. Safely fetch, fast-forward and verify local/remote parity before task work; preserve dirty, diverged and other-owned worktrees and never force reset. Code work stays in scoped worktrees/branches and proceeds through reviewed PRs, required checks and official Actions deployment. Every main push deploys production; because merging this documentation PR also triggers the Worker deployment, disclose that release consequence and follow the authorized merge/deploy path. Sol remains scope/approval/review owner.

Treat **P0–P11 code as complete/PASS**, distinct from deployment and program outcomes. A0.1 PR #283 merged at `22c685e38d46367eec18332f264b7f1480d8305e`; workflow 35822388398 succeeded; Worker version `b5e39685-baa2-4789-acfa-10e7f990d2ad`, build `LU03P9-X1WyePB7inlqsO`; production health passed and exact-main smoke passed 4/4. A0.1 authorized no broader implementation. Gates remain P9–P11 IN_PROGRESS and P12–P13 PENDING; Worker CPU is UNVERIFIED. Do not reimplement completed phases merely because external gates remain open. P9 requires verified live backlink evidence; P10 requires the real production paid-order event; P11 requires reconciliation of delivery records and current live GEO evidence. Never promote a gate from code PASS alone.

There is no A0.2/A0.3 admission, refactor, database change, CREATE, publication or phase promotion. Your first packet is **read-only readiness reconciliation**: confirm source versions, code/deployment/outcome status, remaining blockers and owners, existing authority mappings, and the first eligible packet. Preserve the architecture's program-entry hold. While program gates remain open, explicitly scoped A0.1 documentation-integrity repair and read-only readiness reconciliation are permitted. This does not authorize operational A0.2–A0.7, application implementation, database/provider/account probes, deployment, CREATE/publication, phase promotion or gate bypass. Operational A0 and implementation remain entry-gated unless separately explicitly authorized. Continue authorized read-only work and state the exact decision needed if blocked.

Once eligible, follow A0/A1 and dependency-ordered packets. Retain Modal for heavy compute and Postgres as the sole run/gate authority. Thin Cloudflare requests verify authorization, atomically admit an idempotent durable command and return 202; persisted events power recovery. Reconnect or a lost response must recover the same run, never rerun Ingest or trigger the legacy synchronous fallback. Canary Ingest, then Planner, then GSC before expanding. Queues are optional transport or separately measured bounded execution, not a second scheduler; verify actual account limits, delivery, acknowledgement, retry, retention and cost first.

Enforce **total HTTP Worker CPU <10ms**, including auth, middleware, routing and serialization; retain p95 ≤2.5ms and p99 ≤5ms. Test anonymous, authenticated admin, non-admin, expired/invalid token, cold/warm and key-rotation cohorts. Do not weaken Clerk verification, authorization, RLS or cache isolation. Heavy SSR, crawling, parsing, embeddings and model work stay off request CPU. No waitUntil, timeout increase or standalone HTTP Worker is a CPU-limit escape. Missing CPU telemetry is not PASS; performance remains unverified until measured.

Preserve all existing intelligence, 50-variable, estate ownership, exact slug/canonical, sanitization, cannibalization, qualified YMYL review and release-permit contracts. Jev recommends only within eligible choices; articles remain evidence-led and lead to relevant verified Marketplace targets. No bulk CREATE or publication bypass.

Return a compact readiness report with pinned sources, code/deployment/gate statuses, blockers, proposed Luna packets and acceptance evidence. Then execute only eligible authorized packets through independent review and the official release process. Do not claim production success, ranking gains or 1102 elimination without evidence.
