# Content Studio → 100% Fix Plan (senior eng)

**Baseline:** 42/100 live admin Grok E2E audit (2026-09-04)  
**Bar:** 100% functional Discover → Brief → Draft → Review → Approve → GitHub for `blog_post`, `regional_page`, `article`

## Wave A — P0 (ship blockers) — IN FLIGHT
1. **Placement orphans** — UI rows == readiness denominator; auto-map leftovers
2. **Outline coverage** — outline into generate + audit; no silent desk-only block
3. **Audit & Fix outline path** — completion insert, never heading patches / patch_rejected_twice
4. **Re-audit outline parity** — same evaluate contract as ship

## Wave B — P1 (decision integrity)
5. Persist Grok `ownerProvider`; regen picker must not reset to Entrim
6. Unify SEO analyze action vs writer contract action
7. Region-filter sources + interlinks before Evidence counts
8. Scope Review to current job/brief

## Wave C — Prove 100
9. Live admin E2E ×3 (blog, regional, long-form) on Grok through Approve/GitHub
10. Rescore report; only close when all three ship paths work

## Working model
- Cloud / Grok / DeepSeek implement; senior supervisor reviews diffs, merges, redeploys, re-audits live UI.
